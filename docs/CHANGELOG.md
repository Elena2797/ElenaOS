Estado: conocimiento vigente — se añade cronológicamente, nunca se reescribe
Última verificación: 2026-09-22
Verificado en: git log de isabel-api, life-os-app e isabel-gateway
Fuente de verdad de datos: ninguna

# CHANGELOG.md — Historial relevante

No es un espejo del `git log` completo (para eso, `git log` en cada repo). Aquí solo lo que un chat nuevo necesita saber para entender por qué el sistema está como está.

## 2026-09-26 (issues de Isabel: entregar limpia tareas, reglas Outlook/Calendar, flujo pre-HOTO)

`isabel-api` `7b0ede7` en `main`, 1049/1049. Migración v6 aplicada en Supabase y verificada (insert + CHECK de categoría). Las tools nuevas llegan a Isabel tras `openclaw mcp reload`.
**Conteos provisionales de frescos (26/09 noche, `isabel-api` `f020f24`, 1058/1058):** el catálogo estándar de frescos es el mismo en todos los aviones (los 13 de `SHOPPING_ITEMS`). Un conteo pre-HOTO se guarda como nota `other` con formato "Lemons: 2" (etiqueta exacta; sin migración). Al importar el HOTO se contrasta con `shopping`: igual → `confirmed`; distinto o sin cantidad → `review` con ambos valores. Tablecloths/napkins/DR kits: texto libre, siempre `review`. Checkpoint de `src/mcp.js` movido con nota. **Falta:** `openclaw mcp reload`; FRESCOS completo (tiré/pedí, número único, "Por comprar", pregunta diaria); vista de agenda de vuelos en la app.

**Repaso y cierre de huecos (26/09 noche, tarde-noche):** `isabel-api` `7b76c3a`, `a687c39` (1125/1125); `life-os-app` `4ae24eb` (88/88, guard O5 verde).
1. **Pantalla Fresh Items** (sustituye al "Fresh Items Plan / próximamente"): `/v1/app/fresh` (`core/freshOverview.js`) junta inventario (cantidad, contado vs estimado), Shopping del HOTO, "Por comprar" y el ítem de hoy. La tarjeta de VistaJet enseña "N por comprar" u "Hoy toca contar: X".
2. **Inicio:** "Y N pendientes más hoy" ya NO cuenta lo sin fecha (línea aparte: "N más sin fecha"); la tarjeta "Urgente" enseña todos los dominios con señal bloqueante/sensible al tiempo (el del Core primero).
3. **Guard O5 del frontend re-aprobado** con nota (main.js no importa ningún fixture O5). Será rojo otra vez con cada cambio de `main.js`: el guard por hash de todo el archivo es frágil; si molesta, retirarlo y dejar solo el `doesNotMatch`.
4. **Avisos de feedback con la agenda** (`reminderSlot(now, {landingToday, landingYesterday})`): la tarde nunca antes de 45 min tras aterrizar; el último aviso, antes de las 24 h del último aterrizaje de ayer (1 h de margen). Sin agenda, las franjas de siempre.
5. **"¿Recibiste el avión?"** (`core/aircraftReceived.js`): si la agenda muestra un avión distinto al actual, mañana → pregunta 19:30-21:30; hoy → desde las 08:00 hasta 45 min antes de salir. Una vez por avión+fecha; ella responde con el día y Isabel lo apunta (`vistajet_update_status`).
6. **Revistas:** `hoto_get_details` devuelve `magazines: {to_buy, to_check}` (missing → comprar; needs_renewal → comprobar la edición).
7. Ya estaba hecho y los docs lo daban por pendiente: la tarjeta "Documentos de entrega" (HOTO/Inventario/Laundry) bajo el Copiloto (`b030e7a`), y el aviso de versión nueva de la PWA (`services/appUpdate.js`).
8. **Modo "dejando el avión"** (`core/leavingAircraft.js`, tool `vistajet_leaving_aircraft`, `isabel-api` push del 26/09 noche): solo LEE. Devuelve `todo` (ICAO de dónde lo deja — sugerido por el último vuelo de la agenda —, Cabin Care, defects, offload, Monthly Focus, Shopping a cero, revistas, Fresh Items sin contar, inventario sin contar, Laundry, notas pre-HOTO) y `ready`. Isabel lo lleva de una en una y NUNCA entrega por su cuenta. Sin probar con datos reales.
9. **Pantallas Agenda y Fresh Items vistas** en formato móvil con datos de ejemplo (el HTML real de la app, en el navegador local). Con datos reales siguen sin verse: tras el PIN la app pide el código de Telegram.

**Fresh Items + Agenda de vuelos (26/09 noche):** `isabel-api` `8efe730`, `76c3981`, `d0bcc47`, `cb2b77d` (1099/1099); `life-os-app` `0b8c19f`, `ab570ec`.
1. **Sincronía inventario → HOTO** (`hoto/freshSync.js`, enganchada a `updateItem`): contar un Fresh Item de la categoría "Shopping list ( as required)" (Lemon, Lime, Celery, Green Olives, Orange, Cucumber, Longlife & fresh milk, Types of herbs; descripción EXACTA) escribe el valor en `shopping` del HOTO activo (más de 3 → "+4"). A 0 lanza `runHotoAnalysis` → tarea "Comprar X". Bug corregido el mismo día: leía `session.tail_number` y la columna real es `aircraft_registration`. Sin equivalente en HOTO: Soya milk; Evian/Volvic no tocados.
2. **Verbos** "tiré" (= consumo) y "pedí"/"he pedido" (= pedido) en `batch.js`; el "y" colgante al partir frases se limpia.
3. **Importador Excel** (`services/inventory.js` → `cellText`): nunca guarda "[object Object]". Los 3 de la sesión de julio (NP027-029) son Lee Kum Kee Chilli/Soy/XO (ítems de rotación en Asia), en una sesión ya cerrada: no se tocaron.
4. **Agenda de vuelos:** tabla `vj_flights` (`life-os-app/agenda_migration_v1.sql`, aplicada), `core/agenda.js`, tools `agenda_save`/`agenda_get`, `/v1/app/agenda`, pantalla "Agenda de vuelos" en VistaJet. UTC guardado + zona IANA por aeropuerto → siempre se enseña la hora LOCAL; sin zona válida se rechaza. Las dos fotos (lista y calendario) se unen por (avión, origen, destino, salida UTC); `replace_days` solo con el día entero a la vista; ROT = día de rotación sin vuelos definidos. **La pantalla compila pero NO se ha visto** (pide el PIN).
5. **Pregunta diaria de Fresh Items** (`core/freshAsk.js`, tick de 15 min): un ítem al día en rotación; hora según agenda (45 min tras el último aterrizaje, mín. 10:00; sin vuelos desde las 18:00; nunca después de 21:30).
6. **Pre-HOTO con conteos:** un conteo es una nota `other` "Lemons: 2" que se contrasta con el `shopping` importado.


1. **Entregar avión descarta las tareas del HOTO** (`discardHotoTasks` en `hoto/autoTasks.js`): por título EXACTO de lo que genera el análisis (+ variantes de Cabin Care); no toca tareas escritas a mano. `deliverAircraft` devuelve `hoto_tasks_discarded`; si falla, la entrega sigue ok.
2. **Reglas en descripciones de tools (`mcp.js`):** no replicar Outlook (ya lo ve) y nunca poner vuelos en Google Calendar (su horario de vuelos se ve en LIFEOS). Son instrucciones a Isabel, no bloqueos en código.
3. **Pre-HOTO:** tabla nueva `vj_pre_hoto_notes` (`life-os-app/hoto_migration_v6.sql`, aplicada 2026-09-26), `hoto/preHoto.js`, tools `hoto_pre_add` / `hoto_pre_list` / `hoto_pre_resolve`. Al importar el HOTO oficial (`applyHotoImport`) se contrasta cada nota (determinista, sin modelo): `confirmed` / `not_in_hoto` / `review` (revistas, con el estado oficial). Nunca sobrescribe; migrar o descartar lo decide ella. **Falta:** verlo en la app de LIFEOS (hoy solo por Isabel) y probarlo con un HOTO real.
4. Lección de modelo: no asumir qué significa un icono/número de la app (era nº de pasajeros, no sillas de ruedas).

## 2026-09-25 (el chat de inventario entiende sin depender del LLM — D70 — + cierre real del inventario del 9H-VCF + primer Laundry Form real)

Sesión a contrarreloj: entrega del 9H-VCF el mismo día. Estefanía había llevado un WhatsApp aparte durante tres días porque el modo inventario "no entendía casi nada".

1. **Causa raíz del "No he entendido":** el flujo individual de `/v1/message` mandaba TODA frase al LLM de intención (`intent.js`) y cualquier fallo (respuesta rara, sin ruta de modelo) devolvía `unknown` → "No he entendido". La misma frase ("He usado 2 Evian 0,33 l") fallaba y funcionaba de forma intermitente. En local la llamada al modelo falló entera ("Fallaron todas las rutas para structured_extraction"). Además el resolver adivinaba mal: "Still Water" → Tonic Water (comparten "water"; el promedio 0/1 = 0.5 pasaba el umbral 0.45) y ese consumo llegó a confirmarse; "wine sleeve" → Ruinart.
2. **Arreglo (`isabel-api` `4e1e4e2`, 898/898, desplegado a `main`):** `quickIntent.js` (nuevo, determinista: verbos, cantidades en palabras, "apunta que…", sí/no/confirmo, elección numérica "2"); el LLM queda solo de respaldo y si falla lo dice ("mi intérprete ha fallado", no "no entiendo"). `batch.js`: "X y Y" en una frase hereda el verbo. `resolver.js`: una palabra del query sin encaje descarta el ítem, `dice` sin bigramas repetidos, stopwords, `suggestions` cuando no hay match ("¿quizá Evian 0,33 l?"), y un término genérico ("champagne", "Ruinart") es ambiguo en vez de elegir uno en silencio. Alias: wine sleeve, funda/bolsa de botella, rubber band, liga. El lote enseña ahora `ítem: acción → antes → después` antes de confirmar. 22 tests nuevos (`inventoryNaturalLanguage.test.js`) reproducen los fallos reales.
3. **Inventario del 9H-VCF cerrado con ella, ítem por ítem** (una pregunta cada vez; todo con `buildPatch`/`updateItem`, guarda de valor esperado y relectura): consumos (toallitas flight deck 10→8, Kettle Chips ×2 4→3, cápsulas Illy Classico Espresso y Lungo 30→20, "una caja" = 10), conteos (azúcar rubio 25, Sprite 4, Apple 5, Cranberry 2, Orange 5, Tomato 5, San Pellegrino 0,25 l 5, Dental kits 6, VJ playing cards 4, Volvic 6, Evian 15, VJ cocktail napkins 15, Prist 1, Body Deli Mist 1, bolsas de nueces 0, leche/berries/hierbas/mint 0, lemon 1) y bebidas puestas **al estándar sin contar** por decisión suya (Ginger Ale 2, Tonic 4, Coke Light 6, Coke Zero 4, Coca Cola 6) porque ya no estaba en el avión. **Un error mío, deshecho:** apliqué las cápsulas con una respuesta ambigua ("Un") — revertido a 30 al momento y reaplicado cuando confirmó.
4. **Primer Laundry & Cleaning Form real** (`302ff2d4…`, 9H-VCF, EGKB, 2026-09-25, CH "Elena"): 10 filas Given (Tablecloth S 6, Napkins 8, Microfiber 4, Dinner plate 2, Dessert plate 1, Coffee cup 1, Dessert knife/fork 3, Menu spoon 3, Tumbler 2). Creado por script con las mismas funciones de `laundryCleaning/data.js`, no por `laundry_*` (la primera prueba real de esas tools sigue pendiente).
5. **Fallos de Isabel:** cerrados `58cdac43` (Evian 0,33 l) y `b438615d` (wine sleeve / rubber band). Apuntado el fallo de clasificación de "Prist" (pedido del avión archivado como tarea de Vida Personal).
6. **Descubierto:** las listas de ítems cambian entre aviones (el 9H-VCQ tenía "Nuts - Salted Pistachio Meats", el 9H-VCF no; 349 vs 345 ítems). Que un ítem "no exista" no es un bug: el Excel de la aerolínea se actualiza.

7. **HOTO del 9H-VCF (`a04841c4`) revisado con ella categoría por categoría y escrito con `updateHoto`/`addItem`/`deleteItem`:** cabecera (CH OLE, recibido 22/09/2026, 4 días, ICAO EGKB, patrón P2 — **ella siempre es Pattern 2**), Monthly Focus completado, Stairs cleaned 24/09, Shopping alineado con el inventario (leche 0, desnatada 0, limón 1, limas +4 [inventario 6], naranjas 3, apio 0, hierbas 0, Evian 15), 6 defects (los 2 del PDF + R1/R2 tables, jump seat, manchas en panel del baño, cajón del hielo), Offload (línea vieja "Sores" sustituida por 2 dessert plates en foil), comentarios reescritos (AOG, sin fresh items, 1 limón, uplift 24.09 Nice), revistas cargadas en `shopping.magazines_list` (GQ, Cosmo, Vogue, Nat Geo "Summer Edition" → needs_renewal; The Economist y Time → missing). Full Stock Check queda en 11/09: **nunca marcarlo sin contar todo**. Cerradas las tareas "Rellenar cabecera" y "Enviar feedback HOTO del avión".
8. **Hallazgo — el importador tira las revistas:** `pdfImport.js` lee la celda Magazines ("GQ, Cosmopolitan, Economist, Vogue", texto libre) y la descarta a propósito (para no duplicar `magazines_list`); solo lo deja en `notImported`, que ella no ve. Cada import deja la lista vacía. **Pendiente:** parsear esa celda a `magazines_list` y que Isabel diga qué comprar.
9. **Ideas de producto dichas por ella (sin construir; Isabel SÍ lee capturas, confirmado por ella):** Isabel pregunta "¿recibiste el avión hoy?" y pone la fecha; al decir "dejo el avión" entra en modo preparación y pregunta dónde lo deja y busca el ICAO; pregunta defects durante la rotación; agenda diaria con foto del horario (sin "reporte del día" cuando trabaja); revisar revistas una vez al recibir el avión; marcar en el inventario qué está **contado de verdad** vs estimado/puesto al estándar, para pedirle "hoy cuenta esto"; feedback del avión en 1-2 días (persigue hasta confirmar) y feedback de cada vuelo máximo 24 h.

10. **Bug crítico del export de inventario (corregido en datos, causa raíz pendiente):** la sesión del 9H-VCF (creada 2026-09-23 desde `9H-VCF sept23.xlsx`) tenía `column_map = {code:0, description:1, std_qty:2, req_qty:4}` — desplazado una columna y **sin `current_qty`** — porque la plantilla tiene la columna A vacía (A vacía · B CODE · C DESCRIPTION · D StdQ · E ActQ · F ReqQ). `routes/export.js` hace `colMap.current_qty ?? 0`, así que ActQ no llegaba a su columna: **el Excel exportado salía con los conteos originales, no los de hoy**. Detectado al comparar el Excel con la base de datos. Corregido a `{code:1, description:2, std_qty:3, current_qty:4, req_qty:5}` (mapa antiguo anotado aquí); tras el arreglo 345/345 ítems coinciden con la BD. **Pendiente:** encontrar por qué `parseXlsx`/`invCreateSession` generó ese mapa (¿columna A vacía?) y hacer que `export.js` falle en voz alta si falta `current_qty` en vez de escribir en la columna A. Comprobar el `column_map` de cada sesión nueva antes de confiar en su export.

11. **Entregar un avión (`isabel-api` `cabe137`):** nueva tool `vistajet_deliver_aircraft` (`deliverAircraft`, con dependencias inyectables, 6 tests): cierra HOTO + sesión de inventario + Laundry Form y deja **stand-by sin avión** (no `libre`); exige `confirmed:true` ("hoy dejo este avión" no es una entrega); fail-closed si hay más de un HOTO activo; no toca `rotation_*`. La descripción de `vistajet_update_status` ya no recomienda `libre` para entregar. **Para que Isabel la vea hay que recargar las tools del Gateway** (`openclaw mcp reload`, lo lanza ella). Checkpoints del guard O5 movidos a conciencia (`mcp.js`, `vistajet.js`, y antes `index.js`).
12. **Export con nombre correcto (`cece55d`):** `cors()` ahora expone `Content-Disposition`; sin eso la app no podía leer el nombre y descargaba `inventory.xlsx`. Verificado en producción (`access-control-expose-headers`). El PDF de lavandería en el visor del móvil sigue mostrando "export" (el ticket está firmado para esa ruta exacta): pendiente.
13. **Fase 2 del lenguaje (`aa0c720`, 925/925):** cabeceras "Detrás hay / Tengo adelante" + líneas; **atrás + adelante = total** entre mensajes (mitades en `ctx.partials`, en memoria: se pierden al reiniciar); "no queda ninguna X"/"se acabó X" = conteo 0; "deja X en el estándar"; "me subieron N X" = entrada; "una caja de X" con factor por ítem (`aliases/units.js`: cápsulas Illy = 10; sin factor conocido pregunta, nunca adivina); lo no resoluble de las operaciones nuevas se informa pero **no se aplica ni entra en la cola de excepciones**; sin nada que aplicar no queda propuesta viva que bloquee. Alias del vocabulario real (coca cola normal, diet coke, cubertería dinner=Menu/desert=Dessert, tablecloth S, frescos…). Resolver: palabras de relleno tratadas igual en query e ítem; "agua" ya no cae en Tonic Water (era otra instancia del mismo bug). Comprobado contra los ~50 nombres de sus WhatsApp reales sobre el inventario del 9H-VCF.

14. **Linen → Laundry Form (`3065f0a`):** "usé/gasté N" de un ítem de linen (Napkins, Table cloth small/XS, Microfibre cloth, Cotton tea towel) ya no resta del inventario: suma N "sucias" en `Given` del Laundry Form del avión de la sesión, **acumulando** (`laundryCleaning/dirty.js`, `addDirty`). Fila por descripción exacta, nunca por parecido; linen sin fila (mantas) no se aplica y se dice; formulario inexistente o de otro avión no se toca (D34). Contar linen ("hay 38 napkins") sigue siendo inventario. Sale por el camino de confirmación del lote. 11 tests.
15. **Lo no entendido se apunta como fallo (`58ba464`):** `inventoryFailures.js` — frases con cantidad que ni las reglas ni el LLM entienden quedan en `npm run fallos` con sus palabras tal cual (SISTEMA); distingue "intérprete IA caído" de "vocabulario nuevo"; sin ruido de saludos/preguntas; nunca rompe la respuesta. 7 tests.
16. **Contado vs estimado (`d2147fa`, 958/958):** `verified` = contado de verdad. "Aprox/unos/más o menos N X" y "deja X en el estándar" ya NO verifican: `verified:false` + `notes` que empieza por "estimado". Un conteo real posterior la quita. Consulta sin modelo "¿qué tengo estimado?" / "qué me falta por contar" (`queryService` tipo `estimated`); el estado general cuenta los estimados. **Ojo:** los datos ya escritos el 25/09 (Ginger, Tonic, Coke Light, Coke Zero, Coca Cola al estándar) siguen marcados como verificados; esos ya cerrados no se tocaron. Falta que la app y la tarjeta de confianza (`readiness.js`) usen la distinción.

17. **Tarjeta "Copiloto de entrega" rediseñada + pantalla de VistaJet (life-os-app):** `readiness.js` mide la confianza de entrega con lo que depende de ella (inventario actualizado, Laundry Form, limpieza = tareas diarias del HOTO, documentos y feedbacks); los defectos del avión ya no puntúan; "Por comprar" aparte sin bajar la confianza. Bloque "Documentos de entrega" (Inventario / HOTO / Laundry) justo bajo la tarjeta, en vez de "Exportar Excel" al final de la pantalla. Simulado con los datos reales del 9H-VCF de esa noche: "casi listo" solo por 2 feedbacks y 1 eLearning vencido (el falso positivo de feedback del avión se detectó y corrigió). Ver `modules/AIRCRAFT_READINESS.md`.

18. **Isabel persigue los feedbacks (`2bf7711`, `eee62a6`, 991/991):** `core/feedbackReminders.js`, enganchado a `refreshDomainInterventions` del tick proactivo de 15 min (sin modelo, $0; entrega única por Telegram con plantilla fija). **Feedback del VUELO:** aviso de tarde (19:30-22:00, solo si está de rotación) y "último aviso" a la mañana siguiente (08:30-14:00, también en stand-by si hay pregunta del día anterior pendiente — caso real del 25/09). Estado = `daily_flight_feedback:<fecha>` (respondido → silencio; la respuesta "hecho / hoy no volé" la sigue cerrando Isabel con `lifeos_answer_question`). **Feedback del AVIÓN:** día 1 tras recibirlo por la tarde y día 2 por la mañana ("último día"), con la plantilla del correo (Para HOTO, CC Minseo Kim, Asunto `HANDOVER//<matrícula>//CH-OLE`); fecha de recepción = `received_date` del HOTO activo; la pregunta `aircraft_feedback_due` SOLO se cierra si confirma que YA lo envió ("todavía no"/"luego" no la cierran; `resolveIntervention` lo respeta). Los avisos ya entregados se retiran solos. **Suposición documentada:** no se conoce la hora del vuelo (horario sin integrar), así que el límite de 24 h se aproxima por la mañana siguiente y se respeta el silencio 22:00-08:00.

19. **El import de HOTO ya no tira las revistas (`d698f28`, 1003/1003):** `hoto/magazines.js` interpreta la celda ("GQ, Cosmopolitan, Economist, Vogue") a `shopping.magazines_list`: presentes sin verificar + las de la lista estándar que no vienen como `missing`; respeta estados de nuestro propio export; celda vacía = lista vacía. `mergeImportIntoExisting` conserva la lista puesta a mano. Comprobado de extremo a extremo: el PDF del HOTO exportado el 25/09 se reimporta con sus 6 revistas y estados. Cierra el punto 8 de este día. **Pendiente (Isabel):** que además te diga qué revistas comprar y te recuerde revisarlas una vez al recibir el avión (la lista ya llega con `missing`/`needs_renewal`, falta el aviso).

20. **Triaje del correo de Outlook (`09253d4`, 1039/1039):** el webhook `POST /v1/outlook/inbound` mandaba UN Telegram por cada correo (12-31 al día, entero, la abrumaba). `core/outlookTriage.js`: el correo SIEMPRE se guarda en `eventos`; se avisa solo de lo DISTINTO y con un resumen de 1-2 líneas (nunca el cuerpo). Orden: importancia alta → avisa; remitente automático (noreply/newsletter/marketing) o publicidad (descuentos, ofertas) → silencio; "lo de siempre" (misma plantilla de asunto ≥3 veces en 30 días, o mismo arranque de asunto ≥4) → silencio salvo que el texto hable de un cambio; el resto lo lee el modelo (`outlook_triage`, registrado en `consumerRegistry`, ~$0,0002/correo) y decide + resume. **Falla abierto:** si el modelo o la lectura de la historia fallan, se avisa igual. Repetida sobre sus 100 correos reales: 19 silencio directo, 81 al modelo (casi todo operativo: catering, viajes, crew planning). Solo hay historia desde el 22/09, así que "lo de siempre" mejora con los días. **A vigilar:** cuántos Telegram llegan mañana y si se cuela ruido o se silencia algo importante.

## 2026-09-23, mediodía (Inicio respeta la prioridad real; una tarea por dominio; Cabin Care sin fecha baja a `high` — D71)

Sesión abierta con "revisa cómo me priorizas las tareas": la lista de la mañana era una lista enorme y la app no enseñaba la prioridad. Verificado contra Supabase real (22 tareas abiertas en VistaJet: 5 `critical`, 12 `high`, 5 `medium`). Las tareas `🔴 HOTO:` eran de Isabel a mano, no de D69 (formato de título distinto).

1. **Listas sin orden (`life-os-app` `80c0189`):** `db.js` carga `tasks` sin `ORDER BY` y las listas de cada área, "Todas las tareas" y la frase "Una cosa antes de salir" de VistaJet usaban ese orden crudo. Nuevo `sortByPriority()` (mismo criterio que `tasks_list`) y desempate por fecha/antigüedad en `workQueue()`.
2. **"Ahora" mostraba una `medium` por delante de las `critical` (`144c8d8`)** — bug visto en su captura: `workQueue()` da peso 5 a todo lo que vence en ≤1 día sin mirar `priority`. Ahora desempata primero por prioridad real.
3. **"Ahora" hasta 3 tareas, una por dominio (`80c0189` → `6f39085`)**, con "+N más de <familia>" (`taskFamily()`). Primer intento deduplicando por familia: dejaba a VistaJet con las 3 plazas; ella pidió una por dominio.
4. **Tarjeta "Urgente" (`6f39085`):** etiquetas para `stock_low`, `inventory_discrepancies`, `offload_pending`, `hoto_ambiguous`, `magazine_renewal_needed` y filtro de señales `weak`/`informational`. Antes: "VistaJet: necesita atención".
5. **D69 enmendado (`isabel-api` `4a17738`):** Cabin Care sin fecha → `high` (no `critical`). 876/876. De paso, alias `still water` → evian en `inventoryAliases.js` (el resolver lo reescribió después D70; el fallo `58cdac43` se explicaba sobre todo por el bug de RLS que `df1da17` corrigió 62 min después).
6. **Desplegado y verificado:** Railway (`/health` 200) y Vercel (bundle servido con las cadenas nuevas). El primer `git push` de `life-os-app` lo bloqueó el clasificador ("Out-of-Place Publication"); tras confirmarlo ella en el chat, el reintento pasó.
7. **Sin tocar:** los 2 fallos de PDF (config del Gateway, la lanza ella) y los cambios sin commitear que otra sesión tenía en `isabel-api`.

## 2026-09-23 (memory_search resuelto de verdad + incidente de bucle de reinicio + causa exacta del 401 de PDF)

Continuación de la sesión del 2026-09-22, ahora con acceso a `railway` (CLI ya autenticado como la usuaria) — lo que el día anterior quedó diagnosticado sin aplicar, aquí se aplicó, con un incidente real en medio.

1. **Incidente: Gateway en bucle de reinicio sin SSH, ~20 min.** Un primer intento de arreglar `memory_search` escribió `memory.search.provider` en `openclaw.json` a mano, copiando un esquema de la documentación pública de OpenClaw — que resultó ser de una versión más nueva que la instalada (2026.6.10). Esta versión no reconoce esa clave (`memory: Unrecognized key: "search"`) y el Gateway no arrancaba; como el proceso nunca llegaba a levantar, Railway tampoco daba acceso SSH para deshacerlo. Recuperado publicando un cambio de código (no de config): `isabel-gateway/docker-entrypoint.sh` ahora retira `memory.search` de `openclaw.json` si existe, antes de arrancar — auto-reparación, no destructiva, no hace nada si la clave no está. Desplegado con `railway up` (este repo no tiene remoto git; se despliega así). Isabel/Telegram recuperados.
2. **`memory_search` arreglado de verdad.** Con el CLI ya sano, `openclaw doctor` señaló la causa real para ESTA versión: `agents.defaults.memorySearch.provider` sin configurar cae al default `"openai"`, sin `OPENAI_API_KEY`. Configurado `agents.defaults.memorySearch` (`provider: "openai-compatible"`, `model: "openai/text-embedding-3-small"`, `remote.baseUrl` apuntando a OpenRouter, `remote.apiKey` como SecretRef a `OPENROUTER_API_KEY` — ya existía, sin cuenta nueva) vía `openclaw config patch --file ... --dry-run` primero (valida contra el schema real sin aplicar nada — la lección del incidente anterior). Reindexado (`openclaw memory index --force --agent main`): `Indexed: 2/2 files · 3 chunks`. Verificado con `openclaw memory search` sin error y sin `[memory] sync failed` en logs desde el reinicio.
3. **Causa exacta del 401 de PDF, confirmada contra logs reales (no ya solo hipótesis).** `isabel-api/src/routes/aiProxy.js` es el único punto de salida hacia Anthropic real desde D31/SECURITY#2 — el Gateway ya no tiene la clave real, solo un token de proxy. El log de producción del 18:42 UTC del día anterior mostraba un 401 con `request_id` real de Anthropic: es la `ANTHROPIC_API_KEY` de **`isabel-api`** (no la de `isabel-gateway`) la que Anthropic rechaza. Coincide con lo que ya documentó D68 (rotación de `API_KEY` con `cerrar-clave-publica.ps1`) — esa rotación fue de la API key pública de la app, distinta de la `ANTHROPIC_API_KEY` real de `isabel-api`, que quedó desactualizada por separado. Ambas (`isabel-api` e `isabel-gateway`) actualizadas con una clave real nueva (`sk-ant-api03-...`) — verificado que Anthropic la acepta (un modelo inválido de prueba devolvió `not_found_error`, no `authentication_error`).
4. **Segundo hallazgo tras arreglar la clave: la tool de PDF seguía sin funcionar — el proveedor "anthropic" tenía un perfil de credenciales obsoleto guardado en SQLite que ganaba sobre la clave nueva (env + `openclaw.json`).** Cambiar ese perfil habría roto el fallback conversacional a Sonnet vía el proxy (que necesita el token de proxy, no la clave real, en ese mismo slot compartido). Solución sin tocar esa credencial compartida: `agents.defaults.pdfModel` apuntando a `openrouter/moonshotai/kimi-k2.6` — probado en vivo con `openclaw infer model run` antes de aplicarlo (respondió correctamente), evita el conflicto de credenciales por completo.
5. **Incidente serio durante la prueba en producción: Isabel se coló pidiéndole a la usuaria que reenviara el PDF por Telegram para "importarlo" con una tool nueva (`hoto_import_analyze_pdf`) que esperaba el contenido del PDF en base64.** El modelo intentó emitir el archivo entero (cientos de KB) letra a letra como texto de la llamada a la tool, se quedó en bucle repitiendo la llamada sin terminar nunca, y la usuaria vio basura de protocolo (`<｜DSML｜tool_calls>...`) en el chat. Revertido de inmediato: las descripciones de `hoto_import_analyze_pdf`/`hoto_import_apply_pdf` ahora prohíben explícitamente llamarlas desde una conversación y redirigen a subir el PDF en la app; `hoto_update`/`hoto_add_items`/`laundry_update_items` dejan de instruir a leer el PDF oficial por visión (ver punto 6 de la entrada del día anterior — el mismo riesgo de alucinación ya estaba documentado, pero conservar las tools sin esa salvaguarda explícita en la descripción fue lo que permitió que Isabel las intentara igualmente).
6. **Ese mismo incidente reveló que la sesión de Telegram se quedó bloqueada de verdad** (una llamada `exec` sin terminar nunca dentro del turno atascado) — sus mensajes posteriores dejaron de procesarse en silencio, sin ningún error visible. Diagnosticado con `openclaw channels logs`/`railway logs` comparando `runId`s; confirmado que ni reiniciar el Gateway repetidamente lo resolvía por sí solo (el estado de "trabajo activo" no sobrevive a un reinicio real, así que el síntoma tenía que ser otra cosa — probablemente Telegram limitando la reconexión tras muchos reinicios seguidos en poco tiempo). Pendiente de confirmar que se resolvió solo tras una pausa sin más reinicios.
7. **`life-os-app` estaba en el mismo problema que `isabel-api` al principio de la sesión: 50 commits por detrás de `origin/main`, sin el login real (D62) que la nueva RLS (`lifeos_owner_only`) exige para escribir — por eso "Error creando sesión: new row violates row-level security policy" al intentar abrir una sesión de inventario desde la app.** Confirmado contra el bundle desplegado en Vercel (sin rastro de `link/verify`/`setSession`): la build en producción tampoco tenía el commit. Sincronizado con el mismo método que `isabel-api` (rama de seguridad con el commit local exclusivo, `git reset --hard origin/main`, cherry-pick de los cambios de documentación de esta sesión).

Ver `KNOWN_PROBLEMS.md` § Herramientas nativas de OpenClaw para el detalle completo de los puntos 1-4.

## 2026-09-23, mañana (análisis automático del HOTO crea tareas reales — D69)

Sesión distinta a la de arriba (mismo día): pedido reenviado de Isabel a Claude ("cada vez que el HOTO se actualice, revisa Cabin Care/Shopping/Defects/Offload/Monthly Focus/cabecera y apunta lo que haga falta, sin esperar a que lo pida"). Antes de escribir código se acordaron con ella tres decisiones de diseño (`AskUserQuestion`): disparo en cada actualización del HOTO, deduplicación por "fingerprint" de cada hallazgo, e implementarlo ya mismo.

1. **`isabel-api/src/hoto/analysis.js` (funciones puras) + `hoto/autoTasks.js` (`runHotoAnalysis()`).** Analiza el HOTO activo y sus `vj_hoto_items`, y crea tareas reales con `createTask()` (área VistaJet) — mismo specialist que ya usa `tasks_create`, mismo dedup por título+área, mismo evento de auditoría. Se dispara desde `routes/hoto.js` tras crear/editar/añadir un item/importar, en segundo plano para no añadir latencia al guardado no optimista de la app. Detalle completo de las reglas por sección y del límite conocido de Defects/Offload (se recrea si se completa la tarea sin borrar la línea de origen) en `DECISIONS.md` D69.
2. **27 tests nuevos** (puros + orquestación con Supabase falso en memoria, mismo patrón que `vistajet.orchestration.test.js`/`hotoImport.orchestration.test.js`). Suite completa: 876/876 — el fallo que había antes en `o5DisconnectedGuard.test.js` (`src/mcp.js` desfasado) ya venía resuelto de la sesión anterior de este mismo día (commit `df1da17`, ajeno a este trabajo).
3. **Desplegado:** `isabel-api` `cca4433`, push directo a `main` (sin `railway up`), `/health` verificado 200 tras el push. **No probado todavía contra un HOTO real** — falta que Estefanía edite o importe el HOTO activo y confirme que aparecen tareas nuevas en VistaJet.

## 2026-09-22 (tools de chat del Laundry & Cleaning Form + diagnóstico de 3 problemas reportados en producción)

Sesión disparada por 5 problemas que Estefanía reportó tras recibir el 9H-VCF y enviarle a Isabel el handover (Laundry Form PDF, HOTO PDF, Inventory Checklist Excel) por Telegram. **Nota honesta:** esta sesión trabajó solo sobre los repos locales (`isabel-api`, docs) — no tiene acceso a Railway/`railway ssh`, así que no pudo desplegar nada ni ejecutar comandos contra producción. `CURRENT_STATE.md` y `NEXT_SESSION.md` de esta carpeta no se han tocado: llevan desde 2026-08-10 sin actualizar (esta ruta de `/docs` quedó fuera del ciclo de desarrollo activo, que se mueve por Telegram — ver nota en `README.md` § relación con la raíz) y no hay forma de verificar el estado real de producción desde aquí para reescribirlos con honestidad.

1. **`laundry_get_status`/`laundry_start`/`laundry_update_header`/`laundry_update_items` (tools MCP nuevas)** — `isabel-api/src/laundryCleaning/chatTools.js` + `mcp.js`. El módulo Laundry & Cleaning Form (editor web + export PDF) ya estaba en `main` y desplegado desde antes, pero sin ninguna tool de chat: Isabel no tenía forma de leerlo ni rellenarlo por conversación, solo `isabel_message` (que es del inventario general, un dato distinto). `laundry_update_items` reutiliza `resolver.js` (el mismo matcher por similitud que ya usa `isabel_message`) contra el catálogo real de 78 ítems de `fieldMap.js`. Checkpoint del guard O4/O5 (`o5DisconnectedGuard.test.js`) movido para `src/mcp.js`; **`src/index.js` sigue con un checkpoint desfasado, ajeno a este cambio (ver más abajo)**. 725/726 tests (el único fallo es ese mismo desfase preexistente).
2. **Documentación de los 3 documentos del avión** — nueva sección "Los 3 documentos del avión — no confundirlos" en `modules/VISTAJET.md`, con tabla comparativa (HOTO Checklist / Inventory Checklist / Laundry & Cleaning Form: qué es, quién lo manda, qué tool usa Isabel). También corregido el estado de `modules/VISTAJET_LAUNDRY_CLEANING.md`, que seguía documentado como "no desplegado" pese a estar en `main` desde antes de esta sesión.
3. **Diagnosticados (no corregidos — requieren `railway ssh`, fuera del alcance de esta sesión) dos problemas de herramientas nativas de OpenClaw** — nueva sección en `KNOWN_PROBLEMS.md`: `memory_search` caído por desajuste de proveedor de embeddings (probable efecto colateral del cambio de proveedor de modelo primario a DeepSeek/OpenRouter del 2026-09-21), remediable con `openclaw memory index --force --agent main`; y la tool nativa de PDF sin registrar, con 401 — **no es un problema de que el host carezca de `pdftotext`/`pymupdf`/pip** (confirmado contra la documentación oficial de OpenClaw: la tool no tiene dependencias de sistema), la causa real es que solo se registra con un modelo capaz de PDF con auth verificada — hipótesis principal: `ANTHROPIC_API_KEY` rotada sin propagar a la variable de entorno de `isabel-gateway` en Railway.
4. **Aclarado, no corregido (no es un bug):** por qué no había sesión de inventario abierta para el 9H-VCF. `getActiveSession()` exige matrícula explícita a propósito (D34) y nunca crea una sesión sola — abrir una requiere subir el Excel real de ese avión (con sus `StdQty` reales) desde la app, algo que solo Estefanía puede hacer. Documentado en la nueva sección de `VISTAJET.md`.
5. **Hallazgo colateral, no corregido:** el guard `o5DisconnectedGuard.test.js` ya fallaba antes de esta sesión con `src/index.js changed during disconnected O5` — el checkpoint de `index.js` quedó desfasado por algún cambio anterior a esta sesión (posiblemente el trabajo de login real del mismo día) sin que se actualizara el hash aprobado. No se tocó: no hay contexto de qué cambió en `index.js` ni por qué, así que mover ese checkpoint a ciegas sería exactamente lo que el guard existe para impedir.
6. **Confirmación de feedback por correo (pedido explícito, no parte de los 5 problemas)** — dos confirmaciones puramente conversacionales, sin tabla nueva, reutilizando `interventions` tal cual: (a) `vistajet_update_status` pregunta si ya se mandó el feedback del avión anterior cuando detecta un cambio real de avión dentro de una rotación (`shouldAskAircraftFeedback`); (b) `health_get_sleep_status` compone `getDailyFlightFeedbackStatus()` y pregunta por el feedback del vuelo/vuelos del día, junto a la pregunta de sueño, solo si está en rotación. Ambas se responden con la tool genérica `lifeos_answer_question` — sin tools dedicadas nuevas. Decisiones de diseño confirmadas con la usuaria antes de implementar (`AskUserQuestion`): pregunta única al cambiar de avión, y la pregunta diaria ligada al momento del check de sueño. Tests nuevos: `shouldAskAircraftFeedback`, `decideDailyFeedbackStatus` (`vistajet.test.js`). Detalle en `modules/VISTAJET.md` § 5.

## 2026-09-22, tarde-noche (la app sin API key: SECURITY #2 — D68; turno de noche activo)

- `/v1` deja de depender de `isabel-api-2026`, que iba en el JS público de la app: entra la app con su token de app, los servidores con la llave privada o `API_KEY` (sin valor por defecto), y los PDF con un ticket de 30 minutos (`isabel-api` `2fcdb0b`, `src/core/access.js`; `life-os-app` `6c726b3`, `isabelFetch`). Pedir un código de conexión ya no pide llave: 10 al día como mucho. Los ticks del Gateway pasan a la llave privada en `isabel-gateway` `1c38740`. Ella rotó `API_KEY` con `cerrar-clave-publica.ps1` (a Claude el clasificador no le deja escribir en el Gateway ni en secretos de Railway): la llave vieja da 401 en todo desde las 15:37Z.
- Turno de noche y buscador activados en el Gateway (ella, 14:48Z): cron `turno-noche-0400`, parte de las 08:30 con `night_shift_report`, 48 tools. El `dry_run` del script falló por comillas de PowerShell; el hecho desde Git Bash salió bien (8 trabajos, 7 tareas, 3 búsquedas, 92 s).
- Primer disparo bueno del coach de las 17:00 (15:00:04Z, `delivered`). `npm run fallos`: ninguno abierto.
- La página vieja de inventario de `isabel-api/public/` (chat de inventario con dictado y "Exportar UPLIFT") dejó de funcionar al rotar la clave. Su única función que la app no tenía, **Exportar UPLIFT**, pasa a VistaJet (`life-os-app` `e40f0b1`; `mode=uplift` probado en producción). Ticket de PDF probado en producción con el HOTO de D-AFBS: 200 PDF, y 401 en otro PDF.

## 2026-09-22, noche (Isabel busca en internet — D67)

- Tool privada `web_research` (Perplexity Sonar por OpenRouter, ~0,007 $ por búsqueda, con fuentes, sin datos suyos en la consulta) y una búsqueda por dominio en el turno de noche, guardada como "Investigación". Capacidad `web_search` en el router; el control de gasto cuenta la tarifa por búsqueda. `isabel-api` `a1ad7bc`.

## 2026-09-22, noche (Isabel sincera con sus fallos — D66)

- Nueva tool privada `system_report_failure`: Isabel ya no promete "no volverá a pasar"; dice si falló ella o el sistema y lo apunta en `eventos` (`lifeos:fallo`) para que Claude lo arregle. `npm run fallos` lo lista al empezar cada sesión. `isabel-api` `f1db257`. Probado en producción; el fallo de prueba (la visa, real) se cerró con D64.

## 2026-09-22, noche (turno de noche: Isabel trabaja sola — D65)

- Revisión: Isabel no trabajaba sin que ella le escribiera y no podía leer JETMI ni Marca Propia. Nuevo turno de las 04:00 (`isabel-api` `3ea4b44`, `src/core/night/`): en JETMI, Marca Personal, Marca Propia y Vida Personal ordena tareas y próximos pasos y deja 1-2 cosas listas para usar y una pregunta; VistaJet fuera, en código. Nunca envía, borra, completa ni descarta.
- `POST /v1/night/run` (solo llave privada), tool `night_shift_report`, `GET /v1/app/night`; en la app, "🌙 Isabel trabajó por ti" en esos dominios (`life-os-app` `7ba1c76`). Cron `turno-noche-0400` y parte de las 08:30 en `isabel-gateway` `aa17ecf`, **sin aplicar aún en el contenedor**. Runbook: `operations/TURNO_NOCHE.md`.

## 2026-09-22, tarde ("ya lo hice" cierra tarea y recordatorios — D64)

- La visa de Arabia Saudí siguió en LIFEOS después de que ella dijera que ya estaba: era solo un recordatorio y `tasks_complete` solo miraba tareas. Ahora `tasks_complete` cierra la tarea y sus recordatorios (o solo el recordatorio si no había tarea), y `tasks_list` trae los recordatorios pendientes. `isabel-api` `b0983f7`. Probado con Isabel en producción (D64).

## 2026-09-22, mediodía (Isabel empieza a aprender de lo que ella le cuenta — O5 canary, D63)

- **Encendido:** ella aplicó `knowledge_canary.sql` y puso `LIFEOS_KNOWLEDGE_STAGE=CANARY` (12:57 Madrid). Prueba en producción con turnos reales de Isabel en sesiones aisladas (`openclaw agent --session-key agent:main:o5-proof-*`, sin `--deliver`): las dos frases se guardaron bien, pero en una sesión nueva Isabel **no** consultó lo aprendido y recomendó un vuelo nocturno. Arreglo `isabel-api` `76f70bd`: lo vigente viaja dentro de las tools de estado (solo llave privada). Repetida la prueba: "no lo cojas… sabes que prefieres no volar de noche" y "Gym 1/3 esta semana". Frases de prueba olvidadas después.
- El cambio de los mensajes de coach en `isabel-gateway` (`ffacbcf`) se revirtió (`b9d1a8c`): no hace falta y nunca llegó a producción.

- **Primera entrada viva de O5.** Tools privadas `knowledge_remember`, `knowledge_recall` y `knowledge_forget`: lo que ella afirma de sí misma por Telegram (preferencia, objetivo, compromiso con fecha, límite, hecho, estado) se guarda en el ledger `lifeos:knowledge` de `eventos` con sus palabras, el día y el canal, y Isabel lo consulta después. Deduplicado (identidad semántica + clave idempotente + índice único), con tope diario y sin specialists, FollowUps ni entrega. `isabel-api` `789df11` + `47c35a2` (812/813; el que falla es el conocido de `../life-os-app` fuera del repo).
- **Interruptor `LIFEOS_KNOWLEDGE_STAGE`** (OFF/READ_ONLY/CANARY) y rollback: `operations/O5_CANARY.md`. Desplegado en `OFF`; verificado en producción que las tools existen solo con la llave privada (45 frente a 26) y que en `OFF` no leen ni escriben.
- **La app enseña lo aprendido** en Dominios → "Lo que Isabel sabe de ti", con "Olvidar" (`life-os-app` `b6376a2`, 59/59). El feed de eventos de la app deja fuera las filas del ledger.
- **El Gateway ve tools nuevas sin reiniciarse:** `openclaw mcp reload` (como `node`) hace que el siguiente turno pida otra vez el catálogo al MCP. Comprobado con un turno aislado: Isabel listó las tres `knowledge_*`. Sustituye al reinicio que se hacía para cargar tools (KNOWN_PROBLEMS, coach de las 17:00 del 21).
- **Guard O5:** ya estaba verde en `origin/main` desde que se movió el hash de `index.js` (D55/D57); el rojo de `1e19184` solo seguía en el checkout local atrasado. Ahora comprueba que O5 solo se alcanza por `knowledgeCanary.js`.
- **Docs pendientes de la sesión anterior:** D62 escrita en `DECISIONS.md`; SECURITY #3 y #6 resueltos.

## 2026-09-22, mañana (login real — D62)

- La app entra con el código de Telegram y recibe una sesión real de Supabase; RLS `lifeos_owner_only` en todas las tablas (la aplicó ella). La clave anónima ve 0 filas. `isabel-api` `4197883` + `b74aa53`, `life-os-app` `24f1576`. Detalle en D62.

## 2026-09-21, noche (Marca Personal con Instagram y estrategia propia — D54)

- **Nueva tool `brand_content_context`** (privada). Junta su estrategia de contenido (`areas.ia_context` de Marca Personal), su Instagram de solo lectura (API oficial) y lo que está viviendo, para que Isabel le proponga contenido con datos reales. `isabel-api` `01b40b3`, migración `instagram_credentials.sql` sin aplicar todavía.
- **Conectado el mismo día:** Instagram con un enlace propio (`/oauth/instagram/start`, `isabel-api` `5c3b269`), porque la ventana de tokens de Meta entra en bucle con la verificación en dos pasos. `instagram_credentials` aplicada. Estrategia de ChatGPT cargada, sin datos sensibles. Verificado con sus datos reales, sin errores de métricas.

## 2026-09-21, noche (ON/OFF desde el chat, standby como rotación y MCP que sobrevive a los deploys — D53)

- **La app decía OFF y "Fuera de rotación"** en su primer día de rotación (standby, sin avión). Isabel no podía cambiar el ON/OFF, y la app solo reconocía `rotacion` con avión. Nueva tool `lifeos_set_mode`, un aviso en las tools de VistaJet, y standby enseñado como parte de la rotación.
- **Cada deploy de `isabel-api` dejaba a Isabel sin tools** (sesión SSE en memoria, 404 "Sesión MCP no encontrada o expirada"). Nuevo `POST /mcp/http` sin estado; el Gateway lo usa con `transport: "streamable-http"`.
- `isabel-api` `f3cf0e2`, `life-os-app` `df9c90b`. El hash de `index.js` del guard O5, que fallaba desde el webhook de Outlook (`1e19184`), se movió a conciencia.

## 2026-09-21, última hora (hábitos con racha — D52; OpenRouter en el informe; OAuth viejo retirado)

- **Hábitos con racha (D52):** leer y escribir en días, gym en semanas cumpliendo su objetivo. Tools `habits_status`/`habits_log`, `GET /v1/habits`, y las rachas en los tres mensajes de coach (`isabel-api` `d0a3240`, `isabel-gateway` `a583c3a`). En la app, en la tarjeta 🌱 Hábitos de Vida Personal (`life-os-app` `362a711`).
- **Cada despliegue de isabel-api dejaba a Isabel sin tools** hasta reiniciar el Gateway: el transporte SSE no reconecta. Lo encontró la sesión de D50, que lo resuelve con `/mcp/http` (Streamable HTTP). Hasta que esté, después de cada despliegue hay que reiniciar el Gateway fuera de la hora de un cron.
- **Informe de presupuesto con OpenRouter:** `scripts/budget-status.mjs` enseña lo gastado hoy, en la semana y en el mes, y lo que queda del prepago y del límite de la clave. Avisa con menos de 2 $. Unido a `main` en `993c98e`: el primer despliegue con el Gateway en `/mcp/http` (D53), y Isabel siguió con sus 38 tools. El lector vive en `scripts/lib/`, porque el auditor de rutas de pago no admite en `src/` nada que hable con un proveedor. Primer dato: 0,42 $ gastados de 10 $.
- **OAuth de Gmail de junio retirado de Vercel** (`life-os-app` `d6470c7`): `/api/gmail-auth` y `/api/gmail-callback` dan 404. Su tabla `gmail_tokens` está vacía; queda borrarla.

## 2026-09-21, noche (Isabel lee su Gmail y su Google Calendar — D51)

- **Google conectado:** proyecto de Google Cloud `LIFEOS` con Gmail y Calendar activados, 5 permisos, cliente web nuevo "Isabel API (Railway)" y la app **"En producción"** (en "Prueba" el permiso caduca a los 7 días). Para publicarla, Google exige página de inicio y de privacidad: `isabel-api/public/privacidad.html` (`61d2722`). Cuenta conectada: la suya, verificada con `google_status`.
- **Solo correo de personas** (`df9fabe`, `5ac5bc4`): el primer "sin contestar" eran 7 anuncios de AliExpress de 10. Se descarta lo que trae `Feedback-ID`, `List-Unsubscribe` o `Precedence: bulk`, salvo en hilos donde ella ya escribió.
- **Seguridad (SECURITY.md #12):** con la API key pública de la app se podía leer su Gmail y enviar correos en su nombre. Desde `8c38b4a`, las tools de correo y agenda solo existen con `MCP_PRIVATE_KEY`, que el Gateway usa como `ISABEL_MCP_KEY`.
- **Mensajes de coach:** el de las 08:30 trae agenda y correos importantes; el de las 21:30, lo no contestado con la oferta de dejar el borrador hecho (`isabel-gateway` `0d66964`).
- **El empujón de las 17:00 del primer día no llegó:** empezó a su hora y lo cortó un reinicio del Gateway a las 17:00:14. El sueño de las 08:00 falló por última vez con "Consumidor no reconocido", antes del arreglo de Telegram.
- Tres sesiones trabajaron a la vez en `isabel-api` (Google, D50 y Outlook). Por eso lo de esta se subió desde una copia limpia de `origin/main` y no con `railway up`: un `railway up` habría publicado trabajo a medias de las otras.

## 2026-09-21, última hora de la tarde (Telegram y la app, conectados de verdad — D50)

- **Lo que Isabel apuntaba no se veía en Dominios:** VistaJet solo leía `vj_tasks` y ocultaba `tasks`; el resto de dominios contaba proyectos. Arreglado, y la lista de Tareas vuelve a VistaJet.
- **Home no cambiaba con lo que ella decía:** el Core solo miraba fechas y no se recalculaba al volver de Telegram. Ahora cuentan urgente, para hoy e importante, y Home se recalcula al volver. `/v1/now` reutiliza su respuesta si nada cambió.
- **Isabel puede corregir y descartar tareas** (`tasks_update`, `tasks_discard`, estado `discarded`) y entiende "mañana", "el viernes" o "máx mañana" como fecha. Cuatro tareas completadas por error ese día pasaron a descartadas.
- **Recordatorios en Home**, vía `GET /v1/reminders`, porque `reminders` tiene RLS.
- `isabel-api` `dcff14e`, `life-os-app` `7f5c410` (publicado desde la rama `fix/tareas-isabel-en-dominios`; el `main` local de `life-os-app` tiene un commit O5 sin publicar, `a1f1bee`, que no se tocó). Gateway reiniciado: 33 tools.

## 2026-09-21, tarde (Isabel apunta tareas, cierra inventarios y empuja — D48, D49)

- **Tareas desde Telegram:** tools `tasks_list`, `tasks_create` y `tasks_complete` (`isabel-api` `4b3f7d1`). Escriben en `tasks` con los mismos valores que el formulario de la app y `source: 'isabel'`. Hasta hoy Isabel no tenía ninguna tool de tareas: lo que le contaba se quedaba en la memoria de la conversación, que LIFEOS no ve.
- **Inventarios abiertos (D48):** Isabel ve y cierra, con propuesta y confirmación, las sesiones de inventario que LIFEOS muestra abiertas. Primer uso: las dos de 9H-VCQ de julio.
- **Isabel empuja (D49):** tres mensajes diarios en modo sin piedad (08:30, 17:00, 21:30).
- **Recordatorios a una hora concreta:** tools `reminders_create/_list/_cancel`, tabla nueva `reminders` (`isabel-api/migrations/reminders.sql`, aplicada el 2026-09-21 desde el SQL Editor, con RLS y sin políticas) y cron `reminders-tick-1m` en el Gateway, que llama a `POST /v1/reminders/tick`. La entrega es determinista y cuesta 0 $; es "al menos una vez", con reclamo, reintento y rescate de envíos atascados. `isabel-api` `03abcd4`.
- Calendario: **no hay ninguno conectado**. Ella usa Google Calendar; queda pendiente, junto con los recordatorios a una hora concreta.

## 2026-09-21 (Telegram reparado y primer cerebro fuera de Anthropic — D46, D47)

- **Telegram llevaba roto desde el cambio al proxy**, con "Consumidor no reconocido". Causa demostrada con un diagnóstico de huellas en el 401: OpenClaw presentaba la **clave antigua de Anthropic** (`97e63348…`), guardada en su almacén de perfiles (`agents/main/agent/openclaw-agent.sqlite`, perfil `anthropic:default`), que **manda sobre `openclaw.json` y sobre `ANTHROPIC_API_KEY`**. Arreglado con `openclaw models auth paste-api-key` desde el propio entorno. La afirmación del 2026-09-20 de que "la clave real vive en un solo servicio" era falsa hasta ese momento.
- Tapados por la autenticación salieron tres fallos más, resueltos en el proxy (D46): streaming, `max_tokens` de 32.000 frente a un techo de 2.048, y turnos de ~88 KB. `isabel-api` `b360282`, 630/630.
- **Colisión con otra sesión:** el 2026-09-20 se había subido a `main` un soporte SSE propio (`be52e7c`, `408de8b`) que esta sesión no tenía en local, y un `railway up` desde la copia vieja lo pisó en producción. Se unieron las dos versiones: se conserva `parseAnthropicSseUsage` y su test (que no estaba en `npm test`), y el test que exigía rechazar streaming —roto en `origin/main`— se sustituyó. Lección: `git fetch` antes de cualquier `railway up`.
- **Isabel deja de depender solo de Anthropic (D47):** DeepSeek V4 Flash vía OpenRouter de principal y Claude Sonnet de repuesto. Primer runner real del benchmark (`92ff51a`), smoke de 0,21 $. Mensaje real: 0,0038 $ frente a 0,113 $.
- Trampas de operación encontradas hoy, todas en `operations/INCIDENTE_SALDO_2026-08-11.md` §12: el almacén de perfiles de OpenClaw; ejecutar `openclaw` como root deja ficheros `600 root` que el proceso `node` no puede leer; y cambiar el modelo por defecto no afecta a la conversación viva hasta reiniciar.
## 2026-08-08 (segunda tanda — delivery de Interventions, stale context y el porqué del coste — D38…D40)

- **Delivery de Interventions (D38)** — el eslabón que faltaba. La cadena queda cerrada: `domain signals → global priority → proactive gate → INTERVENTION persistente → DELIVERY → canal → respuesta → apply → reevaluación`. **Verificado de punta a punta en producción** con una Intervention de prueba identificable y limpiable, sin fabricar ningún dato de salud ni de VistaJet: crear → entregar a Telegram (**1 solo envío**) → 2º y 3er ciclo sin reenvío → responder → resolver → reevaluar en silencio → volver a resolver (`no_pending_intervention`) → limpiar (superseded, nunca borrado).
- **Vía oficial, no un hack:** las docs instaladas de OpenClaw 2026.6.10 (la misma versión que corre en producción) documentan la tool `message`, invocable por `POST /tools/invoke` del propio Gateway. Enviar mensajes es `operator.write` — el scope que el adaptador ya tenía; **no** hace falta `operator.admin`, que es lo que bloquea el cron por CLI.
- **Dos primitivas, no una.** `chat.send` conversa (turno de agente, cuesta dinero, responde al llamante); `message` entrega (texto ya decidido, va al canal de Estefanía). Entregar por chat habría hecho que **cada notificación costara un turno de agente**, y los turnos de agente son el 94-98% del gasto.
- **La sesión conversacional NO es la base de datos.** El estado de una pregunta vive en `interventions`; Isabel no tiene que "acordarse de que preguntó". Tools nuevas `lifeos_pending_questions` y `lifeos_answer_question`, que resuelven por `domain`+`kind` contra Supabase. Confirmado en producción: Isabel real, por MCP, lista las preguntas pendientes leyéndolas de la base, no de su transcript.
- **Entrega exactamente una vez, sin migración:** el candado es el índice único que ya existía (`UNIQUE(reason_signature) WHERE status='pending'`), usado como recibo de entrega. Éxito → `answered`; fallo → `superseded`, que libera el reclamo para reintentar y deja rastro.
- **Tres bugs reales, dos hallados por los tests y uno en producción:** (1) el reclamo usaba `createIntervention()`, que **converge a propósito** ante un duplicado — correcto para una pregunta, catastrófico para un candado: los dos procesos creían haber ganado y el mensaje se habría enviado dos veces; (2) `/^s[ií]\b/` **no casa con "sí"** porque `\b` es una frontera ASCII — la respuesta más frecuente en español se habría leído siempre como "no te entiendo"; (3) el adaptador buscaba la config viva en `$HOME/.openclaw` y en esta imagen vive en el volumen: el primer envío falló con `no_allowlist` — **falló cerrado**, que es lo correcto, pero falló.
- **Seguridad del transporte:** una sola ruta nueva y estrecha. `/tools/invoke` es una superficie de operador completo, así que se consume dentro del contenedor donde el token ya vive y nunca se expone. El destino **debe** estar en `channels.telegram.allowFrom` de la config viva: isabel-api no puede elegir a quién escribe, solo a quien el Gateway ya tenía autorizado — y si hay un único destinatario, ni siquiera necesita conocer el chat id.
- **Stale operational context formalizado (D39)** sin columnas nuevas: **current** (pertenece a `vj_state.aircraft`) / **stale-open** (abierto pero de otra entidad) / **histórico** (cerrado) / **superseded** (reemplazado). Una sola pregunta deduplicada por firma —mismos aviones y conteos ⇒ misma pregunta, verificado (`reuse`)— con `push:false`: es real y no urgente, así que **se ve en Home y no se empuja a Telegram**. Responder **no ejecuta**: `applied:false` y una nota que lo dice, porque cerrar sesiones es irreversible y sigue siendo acto suyo.
- **Por qué cuesta lo que cuesta (D40).** Los turnos caros traen `input:3, output:~90, cacheWrite:~25.800`: **el mensaje es minúsculo, lo que se paga es el contexto que se reenvía** (mediana 25.833 tokens). Y **15 turnos escribieron ~383.000 tokens de caché y leyeron 0** — se paga el recargo de escribir (×1,25) y nunca se cobra el descuento de leer (×0,1), porque un cron diario no puede acertar una caché de 5 minutos. Son ~0,29 $ de 1,49 $, tirados. `GET /v1/usage/analysis` lo deja medido de forma continua, con palancas ordenadas por ahorro calculado y declarando cada una qué exige antes de aplicarse.
- **No se ha tocado `cacheRetention` ni el modelo del agente**: son config de OpenClaw con trade-off real (afectan a la calidad y a las ráfagas donde la caché sí sirve; el TTL largo de Anthropic cuesta ×2 escribir). Queda medido y argumentado para decidirlo, no flipeado por intuición. Corregido además un error propio: la palanca de contexto prometía 1,51 $ de ahorro sobre una factura de 1,49 $ porque contaba los espejos de entrega sin prompt.
- **Home muestra las preguntas pendientes**: una Intervention con `push:false` que la app no mostrara sería invisible, y una pregunta que nadie ve es peor que no haberla hecho.
- 392/392 tests en `isabel-api` (25 nuevos), 7/7 en `life-os-app`. Todo desplegado y verificado contra producción real.

## 2026-08-08 (correlación de entidad cerrada, evaluador proactivo y el coste real de Isabel — D34…D37)

- **Auditoría sistemática de correlación de entidad en VistaJet (D34).** Las tres fugas anteriores (D14/D15/D28) se encontraron por accidente; esta vez se recorrieron TODOS los caminos. 14 hallazgos, varios vivos en producción. Los que importan: **`/v1/confirm` resolvía la sesión de inventario sin matrícula** — D28 protegió la propuesta y dejó su confirmación intacta, así que confirmar un conteo escribía el chat (y en batch, la cola de excepciones y el resumen final) contra la sesión de otro avión; **7 de las 9 señales no declaraban `subject`**, verificado en producción, con lo que `dropStaleSignals()` no protegía casi nada; **`dropStaleSignals()` no se llamaba en ningún sitio** fuera de los tests; y en el frontend, **los cuatro loaders caían a "cualquier avión" justo cuando la usuaria entrega el avión** (`aircraft: null` → todos reciben `undefined` a la vez), resucitando el síntoma de D15 por la vía más normal, terminar una rotación.
- **La fuga que llegaba al PDF oficial.** La migración one-time del checklist copiaba `vj_hoto_checks` —clave sin matrícula— al HOTO del avión ACTUAL. D15 corrigió las rutas que la LEÍAN; esta, que ESCRIBE, quedó viva. Retirada, no "corregida": nadie guardó nunca a qué avión pertenecían esos ticks.
- **La corrección es estructural.** Omitir la matrícula ya no devuelve "el más reciente de cualquier avión": **lanza**. El wildcard sigue existiendo pero hay que nombrarlo, lo que además lo deja greppable. Mismo principio que el índice único de D14: la garantía no puede depender de que el código se porte bien.
- **Qué caduca al cambiar de avión:** la presentación, no el dato. Nueva señal `stale_open_context` (sujeto = avión ACTUAL, no el de los restos, o `dropStaleSignals` la descartaría) — destapó al instante las **2 sesiones abiertas de 9H-VCQ** que llevaban desde julio siendo invisibles.
- **Guarda de escritura en el frontend:** `assertCurrentAircraft()` en 12 puntos + refresco en `visibilitychange`. El hueco real era estar en la pantalla del HOTO, cambiar de avión por Telegram, volver sin navegar y seguir escribiendo — ninguna corrección de query lo impide porque no hay query de por medio.
- **Evaluador proactivo general (D35).** `existe` ≠ `merece atención` ≠ `merece INTERRUMPIRME`. Puerta 100 % determinista: fiabilidad → solo `urgent` → evidencia citable → novedad (dedup sobre `interventions`, firma sin contadores) → horario de silencio → cooldown leído de la base, no de memoria. Cada "no" trae su motivo. **Sin cron nuevo y sin tocar el de sueño**: se expone como endpoint dry-run y como tool MCP, para poder observar la alternativa antes de sustituir nada.
- **El punto ciego de instrumentación, cerrado (D36).** Dos errores que la habrían hecho inútil, ambos encontrados verificando en producción: el coste **ignoraba la caché** (un turno real "costaba" 0,00004 $ en vez de 0,087 $ — un número que parece medido y está mal es peor que no tener número), y el barrido buscaba un campo `id` **que no existe** (es `responseId`), reportando `seen: 0` sobre 63 turnos reales **sin fallar**. Además se descubrió que OpenClaw ya reporta `cost` por turno: dato medido, se prefiere a la estimación propia.
- **La respuesta, con datos:** 49 turnos registrados, barrido idempotente (segundo pase: 0 duplicados), y `GET /v1/usage/summary` dice que **los turnos del agente son el 97,8 % del gasto** (1,288 $ de 1,317 $ en 30 días), con 332.957 tokens de escritura de caché frente a 18.209 de input.
- **Coherence pass (D37)** y tres bugs más encontrados mirando la pantalla: la tarjeta de Inventario decía "Al día · Sin novedades" para un avión sin ninguna sesión (derivaba su estado de títulos de tareas, nunca miraba el módulo — misma clase que la señal de Laundry de D15); `(S.vjState||[])[0]` indexaba como array el singleton `vj_state`, así que la actividad de VistaJet salía siempre como "sin actividad registrada"; y `loadIsabelNow()` solo repintaba Home pese a que desde D24 el estado de cada dominio sale del Core, dejando un `—` permanente a quien navegara antes de tiempo.
- **El botón `+` no existía.** D32 quitó el FAB duplicado y no quedó ninguno: `openAdd()` llevaba desde entonces siendo código inalcanzable desde la UI. Restaurado UNO — la lección de D32 era la duplicación, no el botón.
- **Bloqueante de crédito de Anthropic: RESUELTO** (verificado, `/v1/chat` responde). 
- 367/367 tests en `isabel-api` (49 nuevos), 7/7 en `life-os-app` (3 nuevos). Todo desplegado y verificado contra producción real.

## 2026-08-07 (tercera tanda — una sola Isabel de verdad: migración, cutover y contrato universal — D23…D33)

- **Migración blue/green de `isabel-gateway`** al proyecto Railway de `isabel-api`. Railway no permite mover service+volume entre proyectos (confirmado contra su API GraphQL), así que: backup lógico verificado → servicio nuevo sin `TELEGRAM_BOT_TOKEN` (guard anti-409 estructural) → restore del estado → **el cron `sleep-check-0800-madrid` sobrevivió con su id original**.
- **Cutover de Telegram** con ventana verificable de **cero pollers** (evidencia doble: hot-reload aplicado + el Gateway antiguo arrancando con 7 plugins, sin `telegram`). Cero 409. Test de reinicio superado: Telegram, cron, MCP, adaptador y `/v1/chat` sobreviven.
- **`gateway-adapter`**: la red privada de Railway es IPv6-only y OpenClaw no sabe escuchar en IPv6 fuera de loopback (los tres modos probados; confirmado como diseño en su documentación vigente). Se evaluó device pairing remoto (viable, con evidencia del source) y se eligió un adaptador de **tres rutas** con token propio, porque un device aprobado daría el plano de control completo para lo que solo necesita dos operaciones. Threat model en `operations/GATEWAY_MIGRATION.md`.
- **La pestaña Isabel de LIFEOS ya es la Isabel real** — mismo agente `main`, mismas tools, mismo Supabase. Una sola sesión `lifeos`; la superficie viaja como contexto estructurado. Cero secretos del Gateway en el bundle (verificado).
- **Contrato universal de señales** (`core/signals.js`) con **relevancia temporal**: *existe* ≠ *merece atención ahora*. Corrige que `time_sensitive` fuera urgente para siempre — una bolsa pendiente mantenía VistaJet en rojo toda la rotación. Ahora una señal declara cuándo escala y cuándo caduca; el Core lo aplica sin conocer el dominio (test de invariante).
- **VistaJet como segundo cerebro**: señales deterministas reales con severidad, dentro del **motor de prioridad global** (no uno paralelo). `informational` nunca genera aviso.
- **Observabilidad de coste** mínima y auditable sobre `eventos`.
- **`faithful-light` detenido** (reversible): Isabel Core huérfano, Online desde julio con dominio público y secretos de producción, redesplegándose con cada push a `isabel-api`, sin un solo consumidor.
- **Cinco bugs reales corregidos, todos hallados verificando producción**: scope de inventario por avión (D28), carrera en la correlación de respuestas del chat, degradación silenciosa de señales migradas, fuga de jerga interna al texto de Isabel, y la falsa urgencia permanente.
- **Bloqueante detectado al cerrar**: la cuenta de Anthropic se quedó **sin crédito**. Isabel no responde por ningún canal; la capa determinista sigue intacta.

## 2026-08-07 (segunda tanda — ON/OFF conectado, Core decide, contrato de las 4 pantallas — D21/D22/D23/D24)

- **D21 — ON/OFF conectado al priority engine como contexto de desempate.** La usuaria corrigió el framing de D18 ("no estaba pendiente, estaba mal entendido"): `life_context.mode` es su ciclo laboral, nunca el estado del avión, y no se sincroniza causalmente con `vj_state`. `globalContext.js` lo expone como `life_mode` fuera de `classifyAttention()`; el prompt de `now.js` lo usa solo para desempatar dominios ya empatados. 8 tests nuevos verifican que la clasificación determinista es idéntica con ON, OFF y null. Verificado en producción: `life_mode:'ON'` y la prioridad sigue siendo JETMI (`reentry`) porque VistaJet está `clear` — el rango manda sobre el modo.
- **D22 — corrección de D19: el heurístico de cliente deja de calcular prioridad incluso como fallback.** La usuaria detectó que un fallback capaz de producir una prioridad distinta sigue siendo un segundo motor, solo que invisible justo cuando falla el primero. `resolveHomePriority()` ya no calcula nada: usa la decisión del Core, o reutiliza su último resultado válido cacheado (con antigüedad visible), o declara estado neutral sin resaltar ningún dominio. ~30 líneas de decisión de cliente eliminadas de `homeView()`.
- **D23 — investigación cerrada sobre unificar el chat de LIFEOS con la Isabel de Telegram**, leyendo la documentación instalada dentro del contenedor real de producción. Vía soportada: WebSocket del Gateway (`chat.send`/`chat.history` con `sessionId`) vía `isabel-api` como proxy server-to-server. Descartado con evidencia el plugin `admin-http-rpc` (existe y es HTTP, pero su allowlist no incluye `chat.send` — sirve para administrar, no conversar). Bloqueado por una decisión de infraestructura de la usuaria, no por falta de vía técnica.
- **D24 — contrato de las 4 pantallas aplicado.** Home y Avanzar tenían dos colas de trabajo distintas sobre los mismos datos → unificadas en `workQueue()`. Retirada la quinta "voz de Isabel" de Avanzar (era un contador en cliente disfrazado de análisis). El estado por dominio pasa a decidirlo el Core (`attention_mode` de `/v1/now`), eliminando una contradicción visible real (VistaJet decía "EN REPOSO" y "Rotación activa" a la vez). Retirados los metadatos técnicos visibles (etiquetas de PRD y de "especialista"), que además estaban desincronizados del proyecto real. Arreglado "Empezar con esta acción", que para tareas y decisiones hacía `go('home')`.
- **Defecto real encontrado en QA de producción y corregido:** Isabel escribió literalmente "ahora que estás en rotación (life_mode ON)" en una recomendación visible. Regla explícita en el prompt + `checkJargon()`, auditoría no bloqueante con el mismo patrón que `checkLanguage()`. 4 tests, incluida la frase real observada. 225/225 en `isabel-api`.
- Ambos repos desplegados y verificados en producción: `isabel-api` sirve `life_mode` y `jargon_check:ok`; el bundle de Vercel confirma código nuevo presente y heurístico de cliente ausente.

## 2026-08-07 (auditoría arquitectónica completa — single Isabel, Home consolidado, Gym restaurado — D18/D19/D20)

Sesión distinta a D17, mismo día: la usuaria pidió una auditoría arquitectónica seria de todo LIFEOS (canales de Isabel, frontend completo, backend/model routing) con autorización explícita para implementar de forma progresiva lo que resultara seguro y verificable, sin pedir permiso en cada paso.

- **Isabel real confirmada (D18):** `core/ISABEL_CHANNELS.md` estaba fechado 2026-07-10, de antes de que existiera `isabel-gateway` — reescrito con evidencia de código verificada hoy: `isabel-gateway`+Telegram+MCP es el canal conversacional real y único en uso diario (confirmado que el bug de D14 llegó por ahí). `lifeos-agent/agent.py` confirmado abandonado (último commit 2026-06-25). `api/chat.js` y `core/router.js` siguen sin conectar, sin cambios. Brecha real identificada y documentada, no resuelta: el botón "Hablar con Isabel" de LIFEOS sigue usando un bridge local separado (proceso `openclaw` en el portátil) porque `isabel-gateway` corre sin endpoint público — exponerlo es una decisión de seguridad pendiente de la usuaria.
- **Home consolidado a una sola tarjeta Isabel (D19):** existían cuatro cálculos independientes de "qué merece atención" (heurístico de cliente limitado a VistaJet/JETMI, tarjeta beta de `/v1/now`, `attentionItems()`, `domainSignal()|`) — las dos primeras podían señalar dominios distintos como prioritarios. `resolveHomePriority()` + `isabelHomeCard()` (`main.js`) unifican las dos primeras en una sola fuente de verdad (usa `/v1/now` cuando responde, cae al heurístico de cliente si no), y el dominio resaltado en la lista de Dominios usa la misma prioridad. Verificado en navegador (viewport móvil) contra Supabase de producción real — JETMI coincide como prioridad en la tarjeta y en el resaltado, sin errores de consola.
- **"Gym" restaurado como dominio visible (D20):** `gymView()` (sesiones, pesos, restricciones) estaba completo y `regSesion()` ya escribía en `metrics` correctamente, pero `visibleDomains()` lo excluía de la lista — bug, no decisión de producto (sin ninguna referencia a una fusión deliberada con Salud). Añadido a `visibleDomains()`/`domainBlueprint()`/`domainSignal()`. Verificado en navegador con datos reales de producción ("1/2 sesiones esta semana", tarea real de Gym visible).
- FAB de chat duplicado (mismo `openChat()` que el tab "Isabel" del nav inferior) eliminado de `index.html`/`main.css`.
- `core/AUTOMATIONS.md` corregido — decía "todavía sin Telegram activo" a fecha 2026-08-06, pero Telegram se activó esa misma tarde (commits 16:27-16:55). `KNOWN_PROBLEMS.md` actualizado con estos hallazgos y dos gaps nuevos documentados sin resolver: ON/OFF (`life_context.mode`) es decorativo (no prioriza nada, y no está sincronizado con `vj_state.status` aunque responden a la misma pregunta de fondo), y ninguna llamada a Anthropic registra tokens/coste.
- No se tocó: los tres motores de "chat de Isabel" en el frontend (bridge local, chat de Inventario, `/v1/now`) más allá de la consolidación de Home — decidir si se fusionan requiere primero resolver la exposición del Gateway (ver D18).

## 2026-08-07 (tres defectos reales de export/UX del HOTO, encontrados probando D-AFBS en iPhone — D17)

- **Texto duplicado en Tail Number/ICAO:** confirmado inspeccionando el PDF real — el formulario oficial tiene un widget legacy solapado casi exactamente sobre el correcto en ambas celdas. El exportador escribía el mismo valor en los dos, dibujando dos apariencias de texto superpuestas. Corregido: se escribe solo el campo canónico, el duplicado se blanquea explícitamente (nunca queda residuo, ni de la plantilla ni de un export anterior).
- **Magazines no se preservaba al exportar — dos causas reales:** (1) auto-ajuste de fuente (`fontSize(0)`) dependiente del visor, combinado con `NeedAppearances=true`, que le pedía a cada visor recalcular su propia apariencia — soporte desigual para multilínea entre visores; (2) **bug más serio, encontrado verificando contra el dato real de producción**: el texto real de Magazines de 9H-VCQ contiene tabs, y WinAnsiEncoding lanza una excepción al codificarlos — podía abortar `updateFieldAppearances()` a mitad de proceso para TODO el formulario, no solo Magazines. Corregido: tamaño de fuente fijo calculado con métricas reales (nunca "0"/auto) + saneado de caracteres de control en el helper de texto compartido (protege cualquier campo, no solo Magazines).
- **UX de exportación rediseñada como "Guardar PDF":** modal con nombre de archivo editable → Web Share API con `File` real (mejor UX en iOS, conserva el nombre en "Guardar en Archivos") → Blob+`<a download>` → visor nativo como último recurso. Cancelar el Share Sheet no dispara ningún fallback.
- Verificado contra el HOTO real de 9H-VCQ (con sus tabs reales): export ya no lanza excepción, campos duplicados corregidos, round-trip export→reimport coincide campo a campo. 6 tests nuevos (218/218 en total).
- Detalle completo en `DECISIONS.md` D17.

## 2026-08-06 (continuación — flujo real de import de HOTO, D16)

- **Pedido explícito de la usuaria:** poder importar un HOTO real ya recibido (PDF oficial) en vez de re-teclearlo, eligiendo "Continuar este HOTO" (conserva columnas usadas, abre la siguiente libre) o "Nuevo HOTO desde este" (nueva generación, resetea las columnas de handover, conserva avión/stock/cabin care/focus/defects/comments/offload).
- **Auditoría reveló el bloqueo real:** el PDF oficial es un AcroForm real (364 campos, ya mapeados en `fieldMap.js`) — no hacía falta OCR. Pero `vj_hoto_records` solo podía representar UNA columna de handover; `pdfExport.js` limpiaba explícitamente las otras 5 en cada export. El caso pedido ("columnas 1-4 usadas, empiezo en la 5") era literalmente irrepresentable con el modelo anterior.
- **Modelo (aditivo, sin "HOTO v2"):** `columns` jsonb guarda las columnas ya cerradas; los campos planos existentes siguen siendo "la columna de trabajo actual" sin cambios — el editor vivo no se tocó. `generation`/`superseded_by`/`imported_at`/`source_filename` nuevos para la trazabilidad de generaciones, sin tabla nueva.
- **Parser nuevo** (`isabel-api/src/hoto/pdfImport.js`), inversa exacta del exportador, mismo `fieldMap.js`. Verificado exportando el HOTO real de 9H-VCQ y reimportándolo: 100% de los campos coincidieron exactos (defects, offload, comments, 40/40 daily duties, stock, focus). `magazines` se reporta como no reimportable en vez de reconstruirse a ciegas.
- Endpoints `POST /v1/hoto/import/analyze` y `POST /v1/hoto/import/apply`. UI: "Subir HOTO" dentro de `vjHotoView` → "HOTO detectado — X columnas utilizadas" → Continuar/Nuevo → mismo editor de siempre.
- Isabel no necesitó cambios — ya leía `vj_hoto_records` filtrando por `status:'active'`, así que las filas `superseded` (nueva) quedan excluidas automáticamente igual que las `delivered`.
- 21 tests nuevos en `isabel-api` (207/207), incluida orquestación contra PDFs reales construidos con pdf-lib (mismos nombres de campo que el PDF oficial).
- Migración ejecutada y verificada por la usuaria en Supabase (backfill confirmado sobre el HOTO real de 9H-VCQ). Verificación final contra producción real: import→apply→export→reparse con una matrícula desechable vía la API desplegada (100% de los campos, limpiado después), y en navegador contra un HOTO real (pantalla de detección correcta, sin tocar el D-AFBS real).
- **Bug real encontrado en la propia QA, antes de que la usuaria lo viera:** los tres handlers nuevos (`hotoImportFileSelected`/`hotoImportChoose`/`hotoImportCancel`) no estaban registrados en el `Object.assign(window, {...})` que expone las funciones a los `onclick`/`onchange` inline — los tres botones del flujo de import habrían fallado en silencio al pulsarlos. Corregido y verificado en el mismo ciclo de QA antes del deploy final.
- Detalle completo en `DECISIONS.md` D16.

## 2026-08-06 (continuación — el bug de D14 seguía filtrándose: fallbacks de localStorage y una señal desconectada, D15)

- **La usuaria reportó que el bug de D14 NO estaba resuelto del todo:** tras el fix, la cabecera y "Ver evaluación por módulos" ya mostraban D-AFBS correctamente, pero la tarjeta HOTO seguía en "12/47", el resumen de Readiness seguía con "215 items sin verificar; 61 discrepancias" de 9H-VCQ, Inventario seguía mostrando la sesión de 9H-VCQ como "Activa", y Laundry decía "hace 30 días". Instrucción explícita: no dar el bug por cerrado, auditar TODAS las rutas.
- **Causa real, distinta de D14:** no era un problema de correlación por matrícula (esa parte funcionaba). Eran dos rutas de UI que nunca llegaban a pasar por esa corrección: (1) el resumen del checklist HOTO en dashboard/"Estado del avión" caía a `localStorage.vj_hoto_checks` — una clave global sin avión — cada vez que la pantalla viva de HOTO no se había visitado antes en la sesión; (2) la señal de Laundry en `readiness.js` nunca estuvo conectada al módulo real (`vj_laundry_cleaning_records`) — leía `localStorage.vj_laundry_date`/`vj_laundry_items`, claves sin ningún punto de escritura en el código actual, de una implementación anterior a la migración a Supabase.
- **Corregido:** `triggerHotoSummaryLoad()` hace que el resumen del checklist consulte Supabase (correlacionado por avión) de forma independiente de la pantalla viva de HOTO — localStorage queda solo como placeholder mientras carga, nunca como respuesta final. `collectSignals()` deriva Laundry del módulo real (`llcSvc`, correlacionado por avión). "Bajo control" (dashboard y "Estado del avión") ahora exige evidencia real de HOTO+Inventario, no solo ausencia de tareas pendientes.
- Verificado reproduciendo el escenario exacto (ticks de 9H-VCQ sembrados en `localStorage`, contra Supabase real de producción) — sin fugas en dashboard, "Estado del avión" ni Laundry & Cleaning. 4 tests nuevos, primer test runner de `life-os-app` (`node --test`, sin dependencia nueva). Principio formalizado en `PRINCIPLES.md` #11 y `DECISIONS.md` D15.

## 2026-08-06 (continuación — bug real: divergencia Telegram ↔ LIFEOS, vj_state deja de ser singleton "de facto", D14)

- **Bug real reportado por la usuaria, horas después de cerrar VistaJet (D13):** Isabel confirmó por Telegram "avión actualizado a D-AFBS", pero al abrir LIFEOS la pantalla de VistaJet seguía mostrando el contexto completo del avión anterior (9H-VCQ: día 15/17, HOTO 12/47, 215 items sin verificar, 61 discrepancias) como si fuera el actual. Instrucción explícita: no parchear la pantalla, investigar la causa real.
- **Causa raíz:** `vj_state` había acumulado 3 filas en producción (`setup.sql` tenía un `INSERT` incondicional, reejecutado en más de una sesión) pese a documentarse y leerse siempre como singleton. Sin `ORDER BY` en ninguna de las dos lecturas (frontend/backend), Postgres no garantizaba qué fila devolvía — Isabel escribió en una fila, el frontend leyó otra. No era un problema de caché: cada carga sí volvía a consultar Supabase, pero consultaba una tabla rota.
- **Auditoría adicional** encontró el mismo patrón de "reutilizar el dato más reciente sin comprobar a qué avión pertenece" en tres sitios que D13 no había tocado: `vjHotoView`/`hotoReload` (la pantalla viva de HOTO, dejada sin corregir a propósito en D13), y `loadLastSession`/`loadActiveSession` de Inventario (nunca habían tenido correlación por matrícula).
- **Corregido:** datos reconciliados a una sola fila de `vj_state` en Supabase (sin pérdida de dato, verificado antes de borrar); `setup.sql` idempotente; `ORDER BY updated_at` explícito en ambas lecturas; `vj_state_singleton_migration_v1.sql` añade la garantía real a nivel de DB (índice único, pendiente de ejecución manual). HOTO/Laundry & Cleaning/Inventario correlacionados por `vj_state.aircraft` en todos sus puntos de llamada reales. El frontend refresca `vj_state` al entrar/volver al área VistaJet y invalida el resto de módulos aircraft-scoped en memoria si el avión cambió — verificado en vivo simulando una escritura de Telegram con la app ya abierta, sin recargar. HOTO histórico de 9H-VCQ cerrado (`delivered_at` real, provisto por la usuaria: 2026-07-10), sin reasignar su matrícula.
- 10 tests nuevos (187/187 en total), verificado contra Supabase real (`vistajet_get_status` → `aircraft:"D-AFBS", hoto:{exists:false}`, sin fallback al HOTO de 9H-VCQ) y en navegador (dashboard, HOTO, Laundry, Inventario/"Estado del avión"). Detalle completo en `DECISIONS.md` D14.

## 2026-08-06 (continuación — VistaJet cerrado como dominio operativo)

- **Auditados los tres huecos restantes de VistaJet** (proceeding, maleta, administrativo) con el mismo rigor que HOTO en D13 — código real, no suposiciones. Proceeding: cero dato en ningún repo. Maleta: confirmado que las plantillas viven solo en `localStorage`. Administrativo (eLearnings/Facturas): al contrario que los otros dos, **sí** tenía fuente de verdad limpia — `vj_tasks`, la misma tabla ya categorizada por título en `readiness.js` para el dashboard.
- **Construido el mismo día**: `summarizeTaskBucket()` porta esa categorización exacta (mismas regex, mismos criterios) al backend — `vistajet_get_status` ahora incluye `admin: {elearnings, facturas}`. Sin tabla nueva, sin UI nueva, puramente informativo (sin flujo de Intervention, porque resolver una tarea administrativa es una acción en la app).
- **VistaJet queda cerrado como dominio operativo** — los 7 criterios de cierre fijados por la usuaria verificados uno por uno (rotación/avión/entrega coherentes, HOTO correlacionado con historial, passport specialist activo, automatización solo sobre dato fiable, un único punto de escritura por dato). Proceeding y maleta quedan documentados como bloqueados por dato, no como pendientes de esta sesión.
- 184/184 tests en `isabel-api`.

## 2026-08-06 (continuación — modelo de HOTO corregido, D13)

- **Corregido el modelo de HOTO antes de conectarlo a Isabel, no encima de datos inconsistentes.** Auditoría encontró que `vj_hoto_records.status` solo tomaba el valor `'active'` en la práctica (`'delivered'` era un comentario SQL nunca escrito), sin ninguna acción real de cierre/entrega, y sin correlación entre `tail_number` y `vj_state.aircraft` — nada impedía que el HOTO "actual" mostrado fuera de un avión completamente distinto al operativo real.
- **Corregido el mismo día:** `getActiveHoto(tailNumber)`/`loadActiveHoto(tailNumber)` correlacionan por matrícula cuando se les pasa el avión actual (fallback exacto al comportamiento histórico si no, cero regresión en Inventario/Readiness), fail-closed (`{ambiguous:true, matches}`) si hay más de un HOTO activo para la misma matrícula. `closeHoto(id)` implementa la transición real `active → delivered`, conservando la fila (nunca se borra historial). `vistajet_update_status` cierra automáticamente el HOTO del avión anterior al reportar "ya entregué el avión". `vistajet_get_status` expone el HOTO del avión actual como tercera tool del specialist VistaJet.
- Se eligió `delivered` sobre `closed` verificando el vocabulario operativo real del dominio ("entregar el avión"), no por copiar el comentario SQL preexistente — instrucción explícita de la usuaria.
- Cero migración de esquema obligatoria (`status`/`delivered_at` ya existían sin restricción); único cambio de DB es un índice único parcial opcional, aditivo/reversible, verificado sin conflicto contra el único HOTO real en producción antes de escribirlo — queda pendiente de ejecución manual (no hay mecanismo programático de DDL en este proyecto).
- Bug real encontrado y corregido durante el testing: el `state` local podía quedar mutado por el propio harness de test (mutación in-place del fake-db) antes de leer `aircraft` para decidir qué HOTO cerrar — se corrigió capturando el valor explícitamente antes de escribir, más robusto independientemente del mecanismo de persistencia.
- 177/177 tests (18 nuevos), verificado también en navegador contra datos reales (Aircraft Readiness, HOTO, Inventario) sin errores de consola ni regresión.

## 2026-08-06 (continuación — arquitectura multidominio + VistaJet)

- **Auditoría completa de LIFEOS a través de todos los dominios**, seguida de una decisión de arquitectura: la infraestructura común pedida (eventos, estado operacional, interventions, tareas, specialists, identidad/contexto, prioridades) **ya existía** — construida para el vertical slice de sueño, nunca formalizada como patrón general. Documentado en `core/DOMAIN_SPECIALISTS.md` (nuevo) y `DECISIONS.md` D12.
- **Segundo dominio con specialists de Isabel: VistaJet.** Dos vertical slices, ambos sobre `vj_state` (sin tabla nueva): vencimiento de pasaporte (`vistajet_get_status`/`vistajet_update_passport`, con Intervention/dedup igual que sueño) y continuidad operacional (`vistajet_update_status` — rotación/avión/standby, reportado por la usuaria, sin flujo de Intervention porque no hay señal fiable de "dato ausente" sin inventar una). Desplegado en producción, verificado (MCP autenticado, 7 tools presentes, sin regresión en sueño/cron).
- **Bug real encontrado y corregido antes de llegar a producción** (condición de carrera en `getVistajetStatus`: el `reason_signature` de pasaporte está atado a un valor mutable, a diferencia de la fecha estable de sueño — una renovación por otra vía dejaba la alerta "huérfana") y otro durante el testing de `vistajet_update_status` (la limpieza al quedar "libre" se disparaba con el status heredado, no solo con uno explícito).
- **Auditado y explícitamente no construido, con evidencia**: maleta/`bag_checks` (plantillas en `localStorage`, no en Supabase), HOTO recibido/pendiente (`status` de `vj_hoto_records` solo tiene un valor real, `'delivered'` nunca se implementó, no hay correlación con `vj_state.aircraft` — hallazgo nuevo, ver `KNOWN_PROBLEMS.md`), e-learning/facturas (no existe ninguna tabla). JETMI: diseño dejado listo, sin construir, sin datos reales que lo justifiquen todavía.
- 160/160 tests pasando en `isabel-api` (16 nuevos: parser de fechas, lógica de riesgo/decisión de pasaporte, validación de cambios de estado, y escenarios de orquestación contra Supabase falso en memoria).

## 2026-08-06 (continuación — Telegram y cron reales)

- **Isabel operativa 24/7 por Telegram, verificado en producción de punta a punta.** Bot nuevo dedicado `@Isabellifeosbot` (token nunca reutilizado), pareado y endurecido a `dmPolicy: allowlist` con el ID numérico de la usuaria. Cadena Telegram → Isabel (`isabel-gateway`) → MCP `lifeos` → `isabel-api` → Supabase confirmada con una tool real (`health_get_sleep_status`).
- **Flujo de sueño pregunta→respuesta→registro validado con datos reales de la usuaria**, no un test sintético: al probar el camino de lectura, el sueño de hoy (2026-08-06) genuinamente estaba sin registrar; Isabel preguntó por Telegram, la usuaria respondió "8 horas", `health_register_sleep` resolvió la Intervention y creó el CheckIn — sin duplicados, confirmado revisando el transcript completo.
- **Cron diario real creado y persistido:** `sleep-check-0800-madrid`, `0 8 * * *` Europe/Madrid, `health_get_sleep_status` como fuente determinista, guarda silencio si no hace falta preguntar. Precedido de un cron one-shot de prueba que disparó exacto a la hora programada y completó el ciclo cron→agente→MCP→Telegram sin errores ni duplicados; se eliminó tras confirmar.
- **Resuelto el bloqueo de `operator.admin` para gestionar cron por CLI** (documentado desde el 2026-08-05): la Control UI de `isabel-gateway` — deliberadamente no expuesta públicamente — es alcanzable de forma privada vía un túnel SSH (`ssh -L <puerto>:127.0.0.1:8080 <alias>`, usando el alias que genera `railway ssh config`), autenticando con `OPENCLAW_GATEWAY_TOKEN`. Desde ahí, crear/editar/eliminar cron funciona sin el bucle de auto-solicitud de scope que bloquea la CLI.
- **Hallazgo de seguridad nuevo:** `ISABEL_API_KEY` y `ANTHROPIC_API_KEY` aparecen como valores literales resueltos en el `openclaw.json` del volumen de producción, no como referencias `${ENV}` sin resolver — confirmado también por `openclaw doctor`. Ver `SECURITY.md` riesgo #10. No remediado.
- Desliz menor de manejo de secretos: un `railway variables --kv` sin filtrar imprimió `TELEGRAM_BOT_TOKEN` en la salida de una herramienta (no llegó a la respuesta visible a la usuaria). Corregido el hábito para el resto de la sesión (uso de `--kv` siempre con filtro/redacción a partir de ahí).

## 2026-08-06

- **Cerrado el misterio del timeout de cron en Windows: es específico del runtime Windows, no de OpenClaw.** Repro completo en WSL2/Linux nativo (mismo OpenClaw 2026.6.10, mismo modelo, mismo MCP): cron → runner_entered → claude-cli → MCP autenticado → tool → respuesta → run OK, sin fallo. El bug de Windows se explica por un reinicio de Gateway en cascada que cae en modo degradado por falta de integración con Scheduled Tasks — no bloquea Railway/Linux. Ver `KNOWN_PROBLEMS.md`.
- **Fix de seguridad real: `isabel-api/src/mcp.js` ya tiene autenticación.** `requireApiKey` aplicado también a `GET/POST /mcp` (antes era la única ruta sin protección). Desplegado y verificado en producción. Commit `c02d4cd`. Ver `SECURITY.md` riesgo #7 (ahora resuelto).
- **Rediseño y despliegue de la correlación de Interventions: ya no depende de que el LLM recuerde un `intervention_id`.** `health_register_sleep({text})` pierde el parámetro `intervention_id` — LIFEOS resuelve la Intervention pendiente de forma determinista por `domain+kind+status=pending`, fail-closed ante cero o más de una coincidencia (nunca adivina). Incluye fix de una condición de carrera real (dos llamadas concurrentes a `createIntervention` con el mismo `reason_signature` ahora convergen en la misma fila en vez de que una explote por `23505 unique_violation`). 113/113 tests pasando (14 nuevos de orquestación, incluida concurrencia). Migración `kind` aplicada contra Supabase real (columna nueva + índice, sin tocar datos históricos — verificado read-only después). Commit `7ab806f` (isabel-api). Ver `modules/HEALTH_AND_GYM.md`.
- **Primer despliegue real de un Gateway de OpenClaw persistente en Railway: `isabel-gateway`.** Repo y servicio nuevos, separados de `isabel-api`. `gateway.bind: loopback`, sin dominio público, administración exclusivamente vía `railway ssh`. Confirmado funcionando: arranque limpio sin crash-loop, volumen persistente en `/data` (config sobrevive un restart completo), MCP `lifeos` autenticado contra `isabel-api` producción (`openclaw mcp doctor --probe` → `ok`), `claude-cli` funciona al invocarlo manualmente dentro del contenedor. **Bloqueado un turno de agente real**: falta el auth profile interno `anthropic:claude-cli` (distinto del `anthropic:default` de API key, ya registrado) — requiere un login OAuth interactivo que no ha logrado completarse a través de las capas anidadas de `railway ssh`/`su`. Detalle completo, incluido todo lo ya descartado como causa, en `core/AUTOMATIONS.md` y `KNOWN_PROBLEMS.md`. Commits `34b430a`, `de2ab5a` (isabel-gateway, repo local, sin remoto en GitHub todavía).
- **Hallazgo de seguridad nuevo, autoidentificado: `ANTHROPIC_API_KEY` real apareció en texto plano dos veces en la salida de este chat** durante el debugging del despliegue. No remediado (requiere rotación manual desde el dashboard de Anthropic). Ver `SECURITY.md` riesgo #9.
- Sin tocar esta sesión, según instrucción explícita: Telegram (ni bot nuevo ni config), cron diario de las 08:00, `isabel-bridge.js`, `openChat()`, perfil real de OpenClaw del portátil, Control UI (sigue sin exponerse públicamente).
- **Resuelto (sesión siguiente, mismo día): el bloqueo de auth de `isabel-gateway`.** No arreglando el login OAuth de `claude-cli` dentro del contenedor, sino descubriendo que esa vía es arquitectónicamente incompatible con un contenedor efímero — confirmado leyendo el código fuente empaquetado de OpenClaw y su documentación oficial (`docs/providers/anthropic.md`: "Container installs... do not mount host `~/.claude`... use an Anthropic API key there"). Con confirmación explícita de la usuaria, se quitó el override `agentRuntime: claude-cli` del modelo primario (`openclaw config unset` sobre el volumen en vivo + `railway restart`, y en `isabel-gateway/openclaw.default.json`/`README.md` para futuros despliegues). El modelo pasa a autenticar vía `ANTHROPIC_API_KEY` (API key directa), ya configurada. **Un turno de agente real completa correctamente**, verificado en producción (`stopReason=stop`, sin error). Ver `DECISIONS.md` D11. Hallazgo colateral: `railway.exe ssh` no re-escapa argumentos con espacios al reenviarlos por el canal exec de SSH (workaround documentado en `KNOWN_PROBLEMS.md`).

## 2026-08-05

- **Vertical slice real de dominio: Salud/sueño — construido, validado contra Supabase real, commiteado (sin push).** `isabel-api` `96b0d5f`. Dos tablas nuevas (`checkins`, `interventions` — ver `DATA_MODEL.md`), dos tools MCP (`health_get_sleep_status`, `health_register_sleep` — ver `modules/HEALTH_AND_GYM.md`), parser determinista de duración de sueño en lenguaje natural. El parser se corrigió durante la sesión: el diseño original anclaba el patrón a todo el mensaje y no reconocía frases reales como "dormí seis horas y cuarto" — ahora busca una expresión de duración inequívoca dentro del texto, tolerando contexto alrededor sin necesitar una lista de prefijos verbales.
- Validado manualmente de punta a punta contra Supabase real (detección de ausencia, dedup en llamadas consecutivas, interpretación correcta, CheckIn + Evento + Intervention `answered`, reevaluación) y, en un segundo experimento, invocado por un agente real de OpenClaw vía MCP con resultado correcto. Primer CheckIn real conservado: 2026-08-05, 6h15 (375 min).
- **Decisión arquitectónica: OpenClaw se adopta como runtime autónomo de Isabel.** Ver `DECISIONS.md` D10 para el razonamiento completo y `core/AUTOMATIONS.md` para el estado técnico detallado. Auditoría exhaustiva de la instalación real de OpenClaw (v2026.6.10): Gateway, MCP, cron, canales, seguridad, capacidades de despliegue remoto (Railway confirmado como destino documentado y ya usado por `isabel-api`). Despliegue todavía no ejecutado.
- **Hallazgo de seguridad nuevo:** `isabel-api/src/mcp.js` no tiene ninguna autenticación — la única ruta del servidor sin `requireApiKey`. Bloqueante antes de conectar cualquier Gateway remoto. Ver `SECURITY.md`.
- **Hallazgo de seguridad reforzado:** el token de Telegram configurado en el perfil real de OpenClaw es el mismo ya identificado como comprometido en `lifeos-agent` (expuesto en git) — misma incidencia, confirmada ahora en una segunda superficie. Sigue sin remediar, deliberadamente, como incidencia separada.
- Dos experimentos locales, exclusivamente en perfil `dev` aislado de OpenClaw (perfil real, Telegram y producción nunca tocados): (1) crear cron vía CLI con token compartido — bloqueado por scope `operator.admin`, sin workaround de pairing intentado; (2) crear el mismo cron vía Control UI (auto-aprobación de loopback, documentada) — el cron se creó y se disparó correctamente, pero el turno de agente aislado no llegó a completarse (`"cron: isolated agent setup timed out before runner start"`), causa no investigada.
- Sin cambios en `lifeos-agent`, sin cambios en frontend, sin deploy, sin push en ningún repo durante toda la sesión.

## 2026-08-03

- **Isabel Core, Fase 1 (backend, solo lectura) — en producción.** `isabel-api` `2a7b525` — nuevo `GET /v1/now`, montado en `index.js` junto a las rutas ya existentes (no como servicio independiente, ver `DECISIONS.md` D9). Dos piezas nuevas: `core/globalContext.js` (recolector puro de señales cross-dominio desde Supabase — tasks/decisions/waiting_for/alertas/projects/vj_state/vj_tasks/operators — y clasificador determinista de atención `urgent`/`reentry`/`maintain`/`clear`, sin que el LLM decida prioridad a partir de datos crudos) y `core/now.js` (usa esa evidence para pedirle a Claude un headline y una recomendación, con `attention_mode_reliable` para declarar cuándo un fallo parcial de fuente impide confiar en un `clear`). Validado exhaustivamente contra datos reales de producción antes de aprobarse: casos `ok`/`no_signal`/`data_unavailable`/`llm_error`, urgencia sintética real, reentry sin urgencias, fallo de Anthropic, fallo parcial de Supabase. Tres bugs reales encontrados y corregidos durante la validación (fences de markdown de Claude rompiendo el JSON; el atajo "sin evidencia, no llames a Anthropic" nunca se activaba por las marcas `no_signal`; un fallo aislado de tabla podía reportarse como `clear`, un falso negativo de seguridad). `core/router.js` (`POST /chat`, routing a especialistas) sigue sin montar — el bridge local sigue siendo el único canal conversacional activo. Ver detalle técnico en `core/ISABEL_NOW.md` (nuevo).
- **Isabel Core, Fase 2 (frontend, aditivo) — en producción.** `life-os-app` `da840cc` — tarjeta "ISABEL · AHORA" en `homeView()`, consume `GET /v1/now`, convive con la tarjeta estática existente sin reemplazarla. Estados explícitos: `loading`/`ok`/`no_signal`/`data_unavailable`/`llm_error`/`unreachable` (fallo total de red no rompe el resto de Home). Reglas de UX: `reentry` se ve calmado (morado), `urgent` sí tiene tratamiento de alerta (rojo), `attention_mode_reliable:false` se muestra como advertencia explícita. CTA navega al dominio prioritario. Validado en producción real: JETMI aparece como `reentry` ("Retomar"), nunca `urgent`, con `recommendation` que no presenta 36 días de inactividad como emergencia.
- **`life-os-app` `90d9318`** — Decisión `DECISIONS.md` D9 registrada: Isabel Core canónico se integra dentro de `isabel-api` ya desplegado, nunca como servicio independiente. `isabel-api/src/core/index.js` (con su propio `app.listen()`, puerto y API key) queda descartado como camino — estaba scaffoldeado para desplegarse como un quinto proceso Railway, exactamente lo que la usuaria pidió evitar.
- **Dos incidentes reales de infraestructura, encontrados y resueltos en el camino, sin relación con el código de Isabel Core:**
  1. **Railway tenía configurada una región inválida (`sfo`) que bloqueaba silenciosamente todos los deploys nuevos** de `isabel-api` — el servicio seguía "Online" sirviendo el último build bueno, así que no había ningún error visible salvo el mensaje explícito en Settings → Scale. Corregido a `us-west2` (única región de reemplazo que Railway ofrecía). Ver `operations/RAILWAY.md`.
  2. **El webhook GitHub→Railway estaba obsoleto** para `isabel-api`: ni "Redeploy", ni "Latest deploy", ni re-seleccionar la rama conseguían que Railway recogiera el commit más reciente (seguía reconstruyendo el mismo commit antiguo una y otra vez). Solo un **Disconnect + Connect Repo completo** del Source en Settings forzó una resincronización real. Ver `operations/RAILWAY.md`.
  3. **`VITE_ISABEL_API_URL` en Vercel tenía el protocolo mal escrito** (`http://` en vez de `https://`), horneado así en el bundle de producción — Railway rechazaba la conexión HTTP plana con 503, y la tarjeta nueva se veía como "no disponible" aun con el backend sano. Corregido en Project Settings → Environment Variables + redeploy. Ver `operations/VERCEL.md`.
  4. **El service worker de la PWA sirvió bundles viejos cacheados** después de dos deploys distintos de `life-os-app`, aparentando que el fix no había llegado cuando sí lo había hecho. Se resolvió con `unregister()` del SW + borrado de `caches` — ver `KNOWN_PROBLEMS.md`.
- **Pendiente de esta sesión, sin cerrar todavía**: la usuaria validará manualmente en su iPhone (checklist entregado) antes de dar la Fase 2 por completamente confirmada.

## 2026-08-02

- **Bug reportado por la usuaria**: en Dominios no aparecía ninguna tarjeta ("Mapa visual de LIFEOS" se veía, pero la lista debajo estaba vacía), con la píldora de modo mostrando "OFF". Diagnóstico por auditoría de código (sin asumir causa): se demostró que `S.mode` (ON/OFF) no tiene ninguna relación de código con `visibleDomains()` — coincidían porque ambos son el mismo valor por defecto de `S` cuando la carga inicial nunca llega a completarse con éxito.
- `life-os-app` `45738d1` — **Fix: estado explícito loading/loaded/error en la carga inicial de datos.** Causa raíz de código: `reload()` (`src/main.js`) ignoraba el campo `.error` de las respuestas de Supabase y coercía `data:null` a `[]` en silencio — un fallo de red en `ctx`/`areas` era indistinguible de "no hay dominios". `loadAll()` (`src/services/db.js`) pasa de `Promise.all` a `Promise.allSettled` para que un rechazo real en una query no tumbe las otras 12. `reload()` ahora distingue `loading`/`loaded`/`error`, no sobrescribe `S.areas`/`S.mode` si `ctx` o `areas` fallan, y expone un banner de conexión con botón "Reintentar" (`retryLoad()`), visible en cualquier vista. Ver `DECISIONS.md` D8.
- **Incidente real detrás del fallo, investigado y confirmado en la misma sesión**: el proyecto de Supabase (`cllubptdwydifomlnxds`) estaba **pausado** — confirmado por NXDOMAIN en resolución DNS pública (Google DoH), reproducido desde una red independiente del entorno de desarrollo y de este chat, descartando problema de red del dispositivo de la usuaria, de CORS, o de configuración de build (la URL/key horneadas en el bundle de producción coinciden exactamente con `.env.local` y con lo documentado). La usuaria reactivó el proyecto desde su dashboard de Supabase; verificado después: DNS resuelve, la REST API devuelve las 7 filas reales de `areas`, y la app en producción carga Dominios con las tarjetas completas. Ver `operations/SUPABASE.md`.
- Deploy verificado en Vercel para el commit exacto: hash de bundle cambiado (`index-yYQ1Qqcx.js`), contenido confirmado (`retryLoad`, `conn-banner`, `allSettled` presentes), comportamiento confirmado en producción real antes y después de la reactivación de Supabase.
- El push se bloqueó primero por un token de acceso personal de GitHub caducado en el remoto de `life-os-app` (401 Unauthorized, no un problema de red) — la usuaria generó uno nuevo. Reafirma el riesgo #5 de `SECURITY.md`; ese token nuevo pasó en texto plano por este chat, ver nota añadida ahí.
- Detectado de paso, sin corregir (fuera de alcance de esta sesión): la tabla `areas` tiene 7 filas (incluye "Gym") pero `visibleDomains()` solo reconoce 6 nombres — "Gym" no aparece nunca en Dominios. Ver `KNOWN_PROBLEMS.md`.

## 2026-07-14

- `life-os-app` `258d2f5` — Creación de `docs/research/` como categoría de primer nivel de `/docs`: investigación externa por dominio, separada de estado de implementación (`modules/`) y de visión/requisitos (raíz). Estructura: `research/README.md` (convenciones + escala de cobertura NO INVESTIGADO→...→SUFICIENTE PARA DECIDIR + pipeline de ingestión fuente→conocimiento) y `research/JETMI/` (`LOG.md`, `KNOWLEDGE.md`, `HYPOTHESES.md`, `sources/`) como primer dominio.
- **JETMI-D1** (`DECISIONS.md`): JETMI se posiciona como broker de aviación privada, no como plataforma de descubrimiento ni como herramienta para vender leads a otros brokers. Resuelve la contradicción entre `jetmi.md` (raíz, ahora marcado superseded en su propia cabecera) y `JETMI_PRD_Semilla.md`. JETMI sigue en fase de definición/investigación/diseño — no existe sociedad JETMI LDA constituida.
- **Ingestión de los tres primeros artefactos de investigación** en `research/JETMI/sources/`: Manual Operativo del Broker de Aviación Privada (Cap. 1 + índice preliminar de 15 capítulos), Mapa Maestro de Investigación (diagnóstico del índice original, 24 dominios en 7 fases, 7 preguntas estratégicas abiertas), e Investigación 1.1 (41 funciones del día a día de un broker en 11 clusters, mapas de decisión/información, 9 preguntas abiertas). `LOG.md` y `HYPOTHESES.md` actualizados con la cobertura y las preguntas resultantes; `KNOWLEDGE.md` recibió una sección separada de "candidatos a consolidación futura" (6 candidatos), sin promover ninguno a conocimiento oficial.
- `life-os-app` `4a80e9d` — **Auditoría de persistencia y cierre de cuatro huecos de contexto durable**, tras verificación explícita de que los tres artefactos y sus derivados sobreviven sin depender de la conversación: `modules/JETMI.md` documenta la relación JETMI↔Isabel↔LIFEOS (sin especialista implementado) y la restricción de diseño "una sola persona + sistemas/IA progresivos" (sin asignar funciones a agentes); `research/README.md` documenta la cadena completa fuente→investigación→decisión→PRD→implementación→estado verificado y la separación de responsabilidades entre investigación externa, espacio de producto/estrategia y Claude Code; `research/JETMI/LOG.md` fija el próximo bloque de investigación (Fase 1, dominio 1.2 Supply) como decidido, ya no abierto.
- Trabajo puramente documental en los tres commits de la sesión — no se diseñó ni implementó ninguna funcionalidad operativa de JETMI (workflows, agentes, automatizaciones, CRM, modelo de datos), ni se inició la investigación del dominio 1.2.

## 2026-07-10

- **isabel-api** `89151ab` / **life-os-app** `f7d4f86` — Checklist Daily Duties del HOTO conectado de punta a punta: Supabase → 46 checkboxes del PDF oficial mapeados por columna de CH, guardado no-optimista en toda la edición del HOTO, migración one-time de localStorage.
- **isabel-api** `28fd58c` — Fix: el Excel de inventario se nombraba con la fecha de apertura de la sesión, no la de exportación.
- **isabel-api** `5770b7e` — Fix: el parser de lote no reconocía listas en formato "Nombre: cantidad" (número al final) — causaba que listas completas de conteo cayeran en búsqueda difusa y bloquearan al usuario.
- **isabel-api** `c32a253` — Refactor: eliminado código muerto (`CABIN_CARE_LABELS` duplicada y desincronizada del servidor).
- Auditoría completa del módulo HOTO (a petición de la usuaria): se estableció que el modelo actual representa el documento PDF, no la rotación — origen de D6 en `DECISIONS.md`.
- Creación de `/docs` como memoria persistente del proyecto (este mismo cambio).

## 2026-07-07/08

- **isabel-api** `a60bc36` — Módulo HOTO vivo: modelo de datos + exportación al PDF oficial de VistaJet, mismo patrón que Inventario.
- **isabel-api** `36cdb76` — "Isabel Core MVP": código de `/v1/chat` con routing a especialista de inventario. **Nota de esta auditoría: este código nunca quedó montado en `index.js`** — no está activo en producción pese al mensaje de commit.
- **life-os-app** `6be3df4` — Aircraft Readiness: la tarjeta de VistaJet pasa de texto estático a evaluación real basada en datos de todos los módulos (HOTO, Inventario, Laundry, eLearnings, Facturas).
- `ARQUITECTURA_FUSION.md` documenta "dos cerebros, no uno" y propone plan de fusión por fases — plan que a fecha de esta auditoría sigue sin avanzar más allá de la Fase 0.

## Anterior (Sprint 1-2, sin fecha exacta verificada)

- Módulo de Inventario VistaJet: sesiones, parser batch, resolución de ambigüedades, exportación Excel oficial (patch quirúrgico de ZIP) y UPLIFT.
- Estructura modular del frontend con Vite; extracción de acceso a Supabase a `services/db.js`; extracción de comunicación con Isabel a `services/isabel.js`.
