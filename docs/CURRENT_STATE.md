Estado: este documento ES el estado — se reescribe en cada "Cerrar sesión de desarrollo" si algo cambió globalmente
Última verificación: 2026-08-07
Verificado en: cuatro decisiones seguidas en la misma investigación (D14-D17) — vj_state singleton real y verificado en Supabase; import de HOTO real (PDF→LIFEOS) construido, desplegado y en uso real por la usuaria con el HOTO de D-AFBS (Generación 2); tres defectos de export encontrados probando en iPhone e investigados hasta la causa real (uno de ellos, un bug de codificación de pdf-lib con caracteres de control, más serio de lo que parecía a simple vista) y corregidos; 213/213 tests isabel-api + 4/4 tests life-os-app; todo commiteado, pusheado y verificado contra la API ya desplegada en producción
Fuente de verdad de datos: ninguna (agrega, no duplica DATA_MODEL)

# CURRENT_STATE.md — Fotografía del proyecto, ahora

Una página. Solo 5 campos. Detalle de un módulo → `modules/*.md`. Handoff de la última sesión (qué se hizo justo antes, qué archivos tocar) → `NEXT_SESSION.md`, que complementa a este documento, no lo duplica.

## Estado general del proyecto
**Isabel sigue real y funcionando 24/7 por Telegram** (sin cambios de infraestructura esta sesión). El trabajo de estos dos días fue enteramente VistaJet, en cuatro pasadas seguidas: D14 arregló la causa raíz de una divergencia real entre Telegram/Isabel y el frontend (`vj_state` había acumulado 3 filas en producción); D15 encontró que ese fix no bastaba porque dos rutas de UI (un fallback de `localStorage` sin avión, una señal de Laundry desconectada del módulo real) seguían filtrando datos del avión anterior; D16 construyó el flujo real de import de HOTO (PDF oficial → modelo estructurado de LIFEOS, con decisión explícita "Continuar"/"Nuevo HOTO desde este", nunca inferida) porque el modelo solo representaba una columna de las 6 del documento; D17 corrigió tres defectos reales de export/UX que la usuaria encontró probando su HOTO real de D-AFBS en el iPhone — texto duplicado en Tail Number/ICAO (dos widgets solapados del PDF recibían el mismo valor), Magazines rota (auto-ajuste de fuente dependiente del visor, y un bug más serio: texto real con tabs que hace que `WinAnsiEncoding` lance una excepción capaz de abortar la generación de apariencias de TODO el formulario, no solo Magazines), y una UX de exportación rediseñada como "Guardar PDF" con nombre de archivo editable y Web Share API. **El HOTO real de D-AFBS ya está en uso en producción** (Generación 2, con Magazines/defects/comments/cabin care reales cargados) — confirmado observándolo en vivo durante la verificación de D17. El resto de áreas (JETMI, Finanzas, Gym, Marca Personal, Vida Personal) siguen sin lógica de dominio propia.

## Último deploy relevante
`isabel-api` en Railway: commit `842f36c` (incluye D14, D16, D17 — D15 fue puramente frontend). `life-os-app` en Vercel: commit `867a84c` (incluye D14-D17). Ambos pusheados a `main`, auto-deploy activado. El export del HOTO real de 9H-VCQ y del disposable de prueba se verificaron contra la API ya desplegada (no solo local) para D16/D17 — para D15 esa verificación específica de la URL pública de Vercel seguía pendiente (ver `NEXT_SESSION.md` de esa sesión); no se repitió la comprobación explícita esta vez porque D16/D17 sí se probaron contra `isabel-api-production` real vía curl y contra Supabase real. `isabel-gateway` en Railway sin cambios.

## Último commit importante
`isabel-api` `842f36c` — fix de los widgets duplicados + saneado de caracteres de control (D17). `life-os-app` `867a84c` — "Guardar PDF" + docs D17. Ambos con su historial completo de D14-D17 visible en `git log`.

## Bloqueos actuales
Ninguno. No queda ninguna migración SQL pendiente de ejecución manual en Supabase (`vj_state_singleton_migration_v1.sql`, `hoto_migration_v4.sql`, `hoto_migration_v5.sql` — las tres confirmadas aplicadas).

## Siguiente objetivo
Sin instrucción explícita de la usuaria para la próxima sesión — el HOTO de D-AFBS ya está en uso real, así que lo más natural es seguir observando su uso diario por si aparece un cuarto defecto real (probar especialmente el flujo completo "Guardar PDF" → Share Sheet nativo → "Guardar en Archivos" en el iPhone real, que esta sesión no pudo accionar desde el entorno de desarrollo). Pendientes de fondo sin cambios desde D14/D15: cron de sueño de las 08:00 (verificar que sigue corriendo con normalidad), rotación de `ANTHROPIC_API_KEY` (`SECURITY.md` riesgo #9), `SECURITY.md` riesgo #10, y el token de GitHub en texto plano en el remoto de `life-os-app` (ya documentado, re-observado en sesiones recientes).
