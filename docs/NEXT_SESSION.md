Última actualización: 2026-08-07 — handoff corto, no histórico acumulativo (para eso está CHANGELOG.md)

# NEXT_SESSION.md

## Qué se terminó en esta sesión
**Auditoría arquitectónica completa de LIFEOS en dos tandas, con siete decisiones (D18-D24), todo desplegado y verificado en producción.** Primera tanda (D18-D20): se reescribió `core/ISABEL_CHANNELS.md` con evidencia real — la Isabel única y viva es `isabel-gateway`+Telegram+MCP, `lifeos-agent` está confirmado abandonado, `api/chat.js` y `core/router.js` siguen sin conectar; Home pasó de cuatro cálculos independientes de "qué merece atención" a una sola tarjeta Isabel; y "Gym" (pantalla completa, excluida de la navegación por un bug de una lista hardcodeada) quedó visible. Segunda tanda (D21-D24), tras correcciones explícitas de la usuaria: **D21** fijó la semántica canónica de ON/OFF (ciclo laboral, nunca sincronizado con el avión) y la conectó al priority engine como contexto de desempate — `life_mode` se expone en `globalContext.js` deliberadamente fuera de `classifyAttention()`, y el prompt de `now.js` solo lo usa para ordenar dominios ya empatados. **D22** corrigió D19: el heurístico de cliente dejó de calcular prioridad incluso como fallback (un fallback que puede contradecir al Core sigue siendo un segundo motor, solo que invisible); ahora Home usa la decisión del Core, o su último resultado cacheado con antigüedad visible, o un estado neutral honesto. **D23** cerró con evidencia la investigación de cómo unificar el chat de LIFEOS con la Isabel de Telegram, leyendo la documentación instalada dentro del contenedor real. **D24** aplicó el contrato de las 4 pantallas: una sola cola de trabajo (`workQueue()`) compartida por Home y Avanzar, retirada la quinta "voz de Isabel" de Avanzar, estado por dominio decidido por el Core (elimina la contradicción real "EN REPOSO" + "Rotación activa" en VistaJet), y fuera los metadatos técnicos visibles (etiquetas de PRD/"especialista", que además estaban desincronizadas del proyecto real). Además, QA contra producción encontró y corrigió un defecto real: Isabel escribió literalmente "(life_mode ON)" en una recomendación visible — ahora hay regla explícita y `checkJargon()` que lo audita. **225/225 tests en `isabel-api`, 4/4 en `life-os-app`, ambos repos pusheados y verificados en producción.**

## Qué quedó pendiente
1. **Decisión de infraestructura (D23), la más importante:** para que la pestaña Isabel de LIFEOS hable con el mismo agente que Telegram hace falta que `isabel-api` pueda alcanzar a `isabel-gateway`. Hoy no puede: están en **proyectos Railway distintos** y el Gateway corre con `bind:"loopback"`. Recomendación registrada: mover `isabel-gateway` al proyecto de `isabel-api` y usar private networking (`*.railway.internal`), que no expone ninguna superficie nueva a internet. Requiere decisión de la usuaria.
2. **Chat de Inventario:** sigue siendo una implementación conversacional independiente (backend, historial y modelo propios). No retirar hasta que la ruta unificada demuestre que hace las mismas actualizaciones correctamente (instrucción explícita). Contrato de contexto estructurado ya diseñado en `core/ISABEL_SURFACES.md`, sin implementar (implementarlo hoy sería añadir features al bridge congelado).
3. **Gym como dominio vivo:** la pantalla ya es visible, pero sigue sin specialist. Lo pedido (sesiones, fuerza/cardio por semana, check-in proactivo "¿entrenaste hoy?", lenguaje natural por Telegram, histórico) está sin construir — la usuaria pidió no hacerlo todavía en esta pasada.
4. `tasks.suitable_modes` sigue sin conectarse (requeriría UI para marcar una tarea como solo-ON/solo-OFF).
5. Sin registro de tokens/coste por llamada a Anthropic (gap documentado, no bloqueante).
6. Sin cambios: cron de sueño de las 08:00, rotación de `ANTHROPIC_API_KEY` (`SECURITY.md` #9), `SECURITY.md` #10, token de GitHub en texto plano en el remoto de `life-os-app`.

## Qué debe hacerse inmediatamente después
Continuar la auditoría/refactor donde se quedó: quedan sin revisar a fondo la presentación de **VistaJet** y **JETMI** dentro de su vista de área (D24 solo tocó Home, Dominios y Avanzar), el **botón `+`** (`openAdd()`), y dos restos detectados y no abordados: `dashboardView()` sigue siendo una vista huérfana sin ningún punto de entrada, y las tarjetas "eLearnings" y "Facturas" de VistaJet ejecutan ambas la misma acción genérica `openAddVjTask()`. Si la usuaria decide la cuestión de infraestructura de D23, eso desbloquea la unificación real del chat, que es el trabajo de mayor valor pendiente.

## Qué no debe romperse
- **Una sola prioridad canónica, varias representaciones.** `resolveHomePriority()` no calcula prioridad: representa la del Core (fresca o cacheada) o declara que no hay. `domainStatusLabel()` sigue la misma regla para el estado por dominio. Si aparece una señal nueva, se integra en el Core, no como una tarjeta que compita.
- **Una sola cola de trabajo.** `workQueue()` es la única definición de "qué merece mi tiempo" — Avanzar la muestra entera, Home los 3 primeros. No crear una segunda lista.
- **ON/OFF es contexto, nunca evidencia** (`PRINCIPLES.md` #12): no sincronizarlo con `vj_state`, no dejar que un `clear` deje de serlo, no citarlo en texto visible (`checkJargon()` lo audita).
- **Isabel sin más calificación = isabel-gateway+Telegram+MCP.** Capacidad nueva = specialist en `isabel-api/src/core/specialists/`, nunca un chat nuevo. `isabel-bridge.js` y `services/isabel.js` están LEGACY/FROZEN: no añadir features, no retirar hasta que haya sustitución funcional.
- El principio de D15 (`PRINCIPLES.md` #11): corregir una query no basta — auditar cada ruta de presentación. El de D17: un bug "visual" puede tener causa profunda; verificar contra dato real.
- Ningún HOTO histórico se borra; `tail_number` histórico nunca se reasigna; `vj_state` tiene exactamente 1 fila.
- No inventar fechas ni reglas que la usuaria no haya decidido. Secretos solo como env vars en Railway. No reactivar `lifeos-agent` sin rotar sus tokens.

## Qué documentos debe leer el siguiente chat
`README.md` → este documento → `CURRENT_STATE.md` → `DECISIONS.md` D18-D24 → `core/ISABEL_CHANNELS.md` y `core/ISABEL_SURFACES.md` → `PRINCIPLES.md` #11 y #12 → `KNOWN_PROBLEMS.md`. `modules/VISTAJET*.md` solo si el trabajo es sobre VistaJet (sin cambios desde D17).
