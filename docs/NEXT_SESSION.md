Última actualización: 2026-08-07 — handoff corto, no histórico acumulativo (para eso está CHANGELOG.md)

# NEXT_SESSION.md

## Qué se terminó en esta sesión
**Auditoría arquitectónica completa de LIFEOS, pedida explícitamente por la usuaria para consolidar el sistema como su sistema operativo personal e Isabel como su única capa conversacional (D18/D19/D20).** Tres agentes en paralelo auditaron: (1) los canales reales de Isabel hoy, (2) el frontend completo (navegación, todas las superficies de "Isabel", Home, ON/OFF, duplicación de datos), (3) los specialists de backend, model routing y el cron/automation existente. Hallazgos y cambios: **D18** — `core/ISABEL_CHANNELS.md` estaba desactualizado desde 2026-07-10; reescrito con la tabla real verificada: `isabel-gateway`+Telegram+MCP es la Isabel real y única en uso diario (mismo canal del bug de D14), `lifeos-agent/agent.py` confirmado abandonado (sin commits desde 2026-06-25, no reactivar sin rotar sus tokens filtrados), `api/chat.js`/`core/router.js` siguen sin conectar. Identificada y documentada (no resuelta) una brecha real: el botón de chat dentro de LIFEOS sigue siendo un bridge local, una Isabel distinta de la de Telegram, porque `isabel-gateway` no tiene endpoint público — exponerlo es una decisión de seguridad que le corresponde a la usuaria. **D19** — Home tenía cuatro cálculos independientes de "qué merece atención" (heurístico de cliente limitado a VistaJet/JETMI, tarjeta beta de `/v1/now`, `attentionItems()`, `domainSignal()`) que podían señalar dominios distintos a la vez; consolidado a una sola prioridad (`resolveHomePriority()`) y una sola tarjeta Isabel (`isabelHomeCard()`), con `/v1/now` como fuente autorizada y el heurístico de cliente como respaldo instantáneo cuando `/v1/now` no responde. **D20** — "Gym" tenía `gymView()` completo y funcional pero estaba excluido de `visibleDomains()` por un bug (no una decisión de producto) — restaurado como dominio visible. Además: FAB de chat duplicado eliminado, `core/AUTOMATIONS.md` corregido (decía "sin Telegram activo" de forma obsoleta). Todo verificado en navegador (viewport móvil) contra Supabase de producción real, sin errores de consola — no hay tests automatizados nuevos porque los tres cambios son de frontend puro.

## Qué quedó pendiente
1. **Los cambios de D18-D20 no están comiteados ni pusheados.** Son ediciones locales en `life-os-app/src/main.js`, `index.html`, `main.css` y varios documentos de `/docs` — pendientes de revisión de la usuaria antes de commit (esta sesión no recibió autorización explícita para commitear).
2. **Decisión pendiente (D18):** qué hacer con el bridge local del chat de LIFEOS — exponer `isabel-gateway`, retirar el botón, o mantenerlo documentado como segunda vía intencional. No inferible del código existente, requiere decisión de la usuaria (implica seguridad/infraestructura).
3. **Decisión pendiente (`KNOWN_PROBLEMS.md` § Producto):** cómo deben relacionarse `life_context.mode` (ON/OFF) y `vj_state.status` — hoy son dos campos independientes sin sincronizar que responden a la misma pregunta de fondo ("¿estoy trabajando ahora?"). Necesaria antes de usar ON/OFF para repriorizar nada.
4. **El chat de Inventario** (`/v1/message`/`/v1/confirm`, UI propia dentro de VistaJet) sigue siendo una tercera implementación conversacional, no auditada a fondo sobre si debe seguir así o fusionarse.
5. Sin registro de tokens/coste por llamada a Anthropic (gap documentado, no bloqueante).
6. Resto sin cambios de sesiones anteriores: cron de sueño de las 08:00, rotación de `ANTHROPIC_API_KEY` (`SECURITY.md` riesgo #9), `SECURITY.md` riesgo #10, token de GitHub en texto plano en el remoto de `life-os-app`.

## Qué debe hacerse inmediatamente después
Confirmar con la usuaria si se comitean/pushean los cambios de D18-D20. Si no hay instrucción más urgente, continuar el resto del plan de auditoría que la usuaria pidió: revisar las pantallas de VistaJet/JETMI/Finanzas no auditadas a fondo en detalle de UI (la auditoría de frontend fue completa a nivel de inventario, pero D19/D20 solo tocaron Home), decidir las dos preguntas pendientes de arriba, y evaluar si construir un specialist real de Gym (siguiendo el patrón de `health.js`) ahora que el dominio es visible — no empezar sin que la usuaria lo confirme, sigue sin haber decisión operativa de qué constituye una intervención útil para Gym.

## Qué no debe romperse
- **Isabel sin más calificación = isabel-gateway+Telegram+MCP.** Cualquier trabajo futuro que diga "mejorar a Isabel" va en `isabel-api/src/core/specialists/` + `mcp.js`, no en el bridge local ni en los canales muertos — ver `core/ISABEL_CHANNELS.md`.
- **`resolveHomePriority()` es ahora la única fuente de "qué domina resaltar" en Home** — si se añade una nueva señal de prioridad, se integra ahí, no como una tarjeta nueva compitiendo.
- El principio de D15 (`PRINCIPLES.md` #11): corregir una query no basta — auditar también cada ruta de presentación del dato.
- El principio de D17: un bug "visual" puede tener causa más profunda — verificar contra dato real, no solo fixtures.
- Ningún HOTO histórico se borra nunca. `tail_number` de un HOTO histórico nunca se reasigna. `vj_state` debe seguir teniendo exactamente 1 fila.
- No inventar fechas reales cuando falten — preguntar. No inventar reglas de ON/OFF sin decisión explícita de la usuaria.
- Los secretos solo entran como variables de entorno en Railway — nunca en literales de config versionada. No reactivar `lifeos-agent` sin rotar sus tokens.
- No crear una segunda implementación paralela de Isabel — y antes de dar por buena una fusión de "chats de Isabel", confirmar que `isabel-gateway` tiene una vía de acceso real (hoy no la tiene desde LIFEOS/Vercel).

## Qué documentos debe leer el siguiente chat
`README.md` → este documento → `CURRENT_STATE.md` → `DECISIONS.md` D18-D20 → `core/ISABEL_CHANNELS.md` (reescrito entero) → `KNOWN_PROBLEMS.md` § Producto y § Arquitectura → `core/AUTOMATIONS.md`. `modules/VISTAJET*.md` solo si el trabajo es sobre VistaJet (sin cambios esta sesión, D13-D17 siguen vigentes tal cual).
