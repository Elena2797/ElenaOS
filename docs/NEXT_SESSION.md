Última actualización: 2026-08-05 — handoff corto, no histórico acumulativo (para eso está CHANGELOG.md)

# NEXT_SESSION.md

## Qué se terminó en esta sesión
Dos piezas de trabajo independientes. **(1) Vertical slice real de Salud/sueño**, en `isabel-api`, commit `96b0d5f` (NO pusheado): tablas `checkins`/`interventions`, parser determinista de duración de sueño en lenguaje natural (corregido durante la sesión — el diseño original no reconocía frases con contexto alrededor del número, ahora busca la expresión de duración en cualquier parte del texto), especialista `core/specialists/health.js`, dos tools MCP (`health_get_sleep_status`/`health_register_sleep`). Testeado (100/100 tests locales) y validado manualmente de punta a punta contra Supabase real, incluida una invocación real desde un agente de OpenClaw vía MCP. Primer CheckIn real conservado: 2026-08-05, 6h15. Detalle completo en `modules/HEALTH_AND_GYM.md` y `DATA_MODEL.md` § Salud. **(2) Decisión y validación parcial de arquitectura de runtime**: se aprobó que OpenClaw sea el runtime autónomo de Isabel (Gateway, cron, sesiones, canales), con LIFEOS/`isabel-api` como única fuente de verdad — `DECISIONS.md` D10. Se auditó la instalación real de OpenClaw y se ejecutaron dos experimentos locales en un perfil `dev` aislado (nunca el perfil real, nunca Telegram real): administrar cron vía CLI está bloqueado por el scope `operator.admin` incluso con token compartido (confirmado, no hay workaround); administrar cron vía Control UI funciona (confirmado, es la vía a usar). Se descubrió un problema nuevo sin resolver: el turno de agente aislado disparado por ese cron no llega a arrancar. Se encontraron y documentaron dos hallazgos de seguridad nuevos (MCP de `isabel-api` sin auth; el token de Telegram comprometido está también en el perfil real de OpenClaw). Detalle completo en `core/AUTOMATIONS.md`, `SECURITY.md` (riesgos #7 y #8), `KNOWN_PROBLEMS.md`, `CHANGELOG.md` 2026-08-05.

## Qué quedó pendiente
1. **El commit `96b0d5f` de `isabel-api` no está pusheado.** El siguiente chat que toque `isabel-api` debe saber que el remoto está desactualizado respecto al local antes de asumir que "lo que está en Railway" incluye Salud/sueño — no lo incluye.
2. **La causa del timeout del turno de agente aislado vía cron no está investigada.** Es un bloqueante real para confiar en cron de OpenClaw en producción, pero no se investigó porque estaba fuera del objetivo acotado de esa fase del spike.
3. Sin relación con esto, de sesiones anteriores: confirmación de la usuaria tras probar "ISABEL · AHORA" en su iPhone (checklist entregado el 2026-08-03, sigue sin respuesta); "Gym" no aparece en Dominios (`KNOWN_PROBLEMS.md`); investigación de JETMI en pausa, próximo bloque decidido es Fase 1 dominio 1.2 Supply, pendiente de que la usuaria traiga el artefacto (`research/JETMI/LOG.md` § 6).

## Qué debe hacerse inmediatamente después
No hay ninguna acción de oficio pendiente de iniciativa propia. Antes de avanzar cualquier fase nueva del plan de convergencia de OpenClaw (push de `96b0d5f`, investigar el timeout de cron, autenticar el MCP, desplegar en Railway, rotar el token de Telegram), **pedir autorización explícita** — todo el plan se ha ejecutado hasta ahora estrictamente fase a fase con aprobación previa de la usuaria, y esta sesión terminó en medio de ese plan, no al final de él. Si la usuaria confirma la prueba de iPhone pendiente de antes: no hay acción de oficio tampoco, preguntarle qué quiere decidir a continuación.

## Qué no debe romperse
- El commit `96b0d5f` de `isabel-api` no debe pushearse sin autorización explícita nueva — sigue siendo trabajo local intencionalmente no publicado.
- Los tokens comprometidos (GitHub, Telegram) siguen siendo una incidencia abierta sin remediar: no imprimirlos, no reutilizarlos, no copiarlos, no moverlos a Railway, no usarlos en pruebas, no introducirlos en archivos nuevos. Si se encuentra otro secreto expuesto: parar antes de cualquier commit y reportarlo.
- El perfil real de OpenClaw (`~/.openclaw`) y Telegram real no se tocan sin decisión explícita — todo el spike se hizo en el perfil `dev` aislado (`~/.openclaw-dev`) a propósito.
- `isabel-api/src/mcp.js` sigue sin autenticación — no conectar un Gateway remoto a él hasta resolver eso (`SECURITY.md` riesgo #7).
- Las tablas `checkins`/`interventions` ya tienen datos reales (el CheckIn de 2026-08-05) — cualquier cambio de esquema sigue el protocolo antes/después (`PRINCIPLES.md` #5 y #7), no un TRUNCATE por conveniencia.
- El resto de "qué no debe romperse" de la sesión anterior sigue vigente sin cambios: `attention_mode`/`can_ignore` deterministas, `attention_mode_reliable` no debe mentir en fallo parcial, la tarjeta "ISABEL · AHORA" es aditiva, cero writes desde Isabel Core más allá de lo ya aprobado, `core/router.js` sigue sin montar, región de Railway en `us-west2`, `VITE_ISABEL_API_URL` correcta en Vercel, ningún cambio de esquema en Supabase sin protocolo, los documentos de la raíz del proyecto no se editan desde `/docs`.

## Qué documentos debe leer el siguiente chat
`README.md` → `CURRENT_STATE.md` → este documento → si toca Salud/sueño o el especialista: `modules/HEALTH_AND_GYM.md` + `DATA_MODEL.md` § Salud → si toca OpenClaw, cron, o cualquier plan de runtime autónomo: `DECISIONS.md` D10 + `core/AUTOMATIONS.md` (ambos con el estado exacto de qué está probado y qué no) → si toca seguridad o antes de cualquier despliegue remoto: `SECURITY.md` (riesgos #7 y #8 son nuevos) + `KNOWN_PROBLEMS.md`. El resto de documentos (Isabel Core, Railway, Vercel) sigue igual que el 2026-08-03, sin cambios esta sesión.
